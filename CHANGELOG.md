# 更新日志

## [3.9.0] - 2026-09-25

### 新增
- **祈愿试炼（组队）** — 一整套新玩法，`祈愿 / 祈愿组队 / 祈愿加入 / 祈愿退出 / 祈愿我的 / 祈愿随机 / 祈愿管理` 七条指令。凑满一队即自动「发车」，每位成员获得一条**以队名命名的状态**（默认 `wish_status_days` = 3 天），出现在「查询」里。**除此之外没有任何数值产出**——不发分、不发道具
  - **队名由系统生成**：`%m月%d日` + `祈愿试炼`，而日期是**开团日 + 状态天数**（即这条状态到期的日子）。玩家不能自取队名（`祈愿组队` 只收一个人数参数），只有诸神能用 `祈愿管理 重命名` 改
  - **3 天窗口内名单是活的**：发车不是终点。移出/解散会**撤销**对应状态，补位/换人会按**队伍的到期时间**挂上（补位者与队友同一天到期），`延期` 改的是队伍的到期时间并同步全队。队伍 `expire_at` 是成员状态的唯一事实来源
  - **名额控制节奏**：名额按**队名日期**（= 开团日 + 状态天数）的星期算，默认表 `wish_weekly_limits = [1,1,0,1,1,0,2]` → 实际效果是**周三与周日不能开团、周四 2 支、其余 1 支，每周最多 6 次发车**
  - **只有成功发车才占名额**：开团不占名额；名额为 0 或已用尽时开团与加入都会被直接劝退；某队发车用尽名额后，同日其余还在招募的队伍被立刻判为「未能成行」并播报
  - 其余约束：每人每天开团 1 次、每群每天 4 次（被前置检查拒绝的不计数）；同一玩家同一名额日期只能参与一次；同时只能在一支进行中的队伍里；身上已有同名状态时不能加入同名队伍；招募等待 45 分钟、每 15 分钟催一次缺人提醒、超时自动解散（空队不播报）；同一玩家最多保留 3 条队伍状态；已结束队伍记录保留 7 天
  - **开团/发车/换人/超时解散都会向群里播报**——没有播报就没人知道有队伍在招募，这个功能根本不成立
- **`祈愿管理`（诸神级）** — `列表`（不含已解散）/ `已解散` / `名单 <队名> [登神分] [觐见分]` / `换人 <玩家1> <玩家2>` / `解散` / `重命名` / `移出` / `补位` / `延期` / `清空`（需加「确认」）。权限是**诸神级**（超管 ∪ 白名单诸神 ∪ 本群群主/群管），比「天梯榜管理」宽——队伍治理按设计交给诸神；功能关掉或群从 `wish_groups` 移除后它仍可用，以便收拾遗留队伍
- **`名单` 接上既有的录分流程** — 不给分数只列队员名；给了分数则输出**可以直接粘贴进「批量录入」的文本**（每个成员一块 `【玩家：甲】【登神之路+16】【觐见之梯+2】`），诸神办完试炼只需填两个数字。分数仍由诸神决定，功能不替他做决定
- **`重命名状态 <玩家名> <旧状态名> <新状态名>`** — 改状态名并保留剩余期限与阻断项；目标名已存在时**合并**为一条（到期时间取更晚的、阻断项与来源保留目标行原有的）。`祈愿管理 重命名` 对已发车队伍复用的就是这条能力
- **`player_statuses.source` 列** — 记状态来源（`'wish'` = 祈愿试炼写入，NULL = 诸神添加）。队伍状态与诸神手工状态在表里同名同形，只靠这一列能分辨；「最多保留 N 条队伍状态」「改队伍名时只改队伍那一条」都依赖它。老库启动时自动补列
- **退群自动移出队伍** — 接在既有的群成员变动事件上，**独立于 `auto_whitelist_group`**（那段逻辑没配待审群时会提前返回）。只处理已绑定 QQ 的玩家：事件给的是 QQ 号，未绑定者不做名片猜测（猜错会把别人踢出队伍），那部分仍靠超时回收
- 新增配置项 `feature_wish_enabled` / `cmd_wish` / `cmd_wish_admin` / `wish_groups` / `wish_default_capacity` / `wish_max_capacity` / `wish_name_max_length` / `wish_status_days` / `wish_status_keep` / `wish_list_limit` / `wish_daily_create_limit` / `wish_daily_group_create_limit` / `wish_weekly_limits` / `wish_reminder_minutes` / `wish_disband_minutes` / `wish_history_retention_days` / `wish_cooldown_seconds` 与 6 个 `wish_messages_*` 文案池。**`wish_groups` 默认空 = 不在任何群启用**，要填群号才生效
- 状态阻断多了 `祈愿` 一项（`添加状态 张三 沉默 2 阻断=祈愿`）

### 变更
- 新增 `WishService`（`wish_service.py`）与 `WishCommandsMixin`（`commands/wish.py`）；调度器新增 `wish_tick`（每 30 秒）与每日清理回调 `purge_old_wish_teams`；`purge_daily_tables` 顺带清理两张新的每日计数表
- `_group_member_change_impl` 重构：白名单自动同步仍然只在 `auto_whitelist_group` 指定的群里生效（群号判断留在调用侧，因为 `_handle_auto_whitelist` 自己不看群号），而退群移出队伍对所有启用的群生效；同时不再要求配了待审群才记录会话串
- `split_status_rename` 提升为 `text_utils.split_rename_pair`，供「重命名状态」与「祈愿管理 重命名」共用
- `_wish_send` 沿用赌局那套「策略层 + 纯发送通道」分离：被群访问控制排除的群一律不发，且策略放在可测试的那一层

### 修复
- **不走 `add_status` 挂队伍状态**：`add_status` 的 upsert 是 `expire_at = excluded.expire_at` 无条件覆盖，会把一条更晚的同名状态**写短**（还剩 10 天的惩罚会被写成 3 天）。队伍状态改用 `set_wish_status_until`（`max(expire_at, excluded.expire_at)`，且不触碰 `block_actions`/`source`），并在加入时先做同名检查
- **队员排序带 `rowid`**：`joined_at` 只有秒级精度，一支队伍通常在几秒内凑齐，同一秒加入的人若只按 `player_id` 兜底就退化成字符比较——「队长交接到最早加入的剩余成员」会挑错人

### 升级注意
- 本版**不改既有表结构**，只新增四张表（`wish_teams` / `wish_team_members` / `wish_daily_creates` / `wish_daily_group_creates`）与 `player_statuses.source` 一列，全部幂等
- 祈愿试炼**默认哪个群都不生效**：要用请在 `wish_groups` 填群号。`wish_weekly_limits` 默认表已按「周三与周日不开团、每周最多 6 次」配好，改 `wish_status_days` 会让整张表跟着平移
- 默认容量 6 人、等待 45 分钟：小群请把 `wish_default_capacity` 调小（玩家也可以在 `祈愿组队` 里自己写人数），否则队伍会凑不满然后超时解散

### 测试
- 新增 `tests/test_wish_db.py`（数据层：开团自动入座、满员发车全员同一到期时间、名额（关闭日/用尽/抢不到/宣判同 slot）、补位与队友同期、换人转移状态、移出/解散撤销、延期只延不缩、改名同步状态名、退群移出、清空释放名额、四个状态原语）
- 新增 `tests/test_wish_concurrency.py`（名额只认领一次、容量不被并发突破、同一玩家并发加入只进一次、两项每日上限不被突破、换人与移出并发不漂移、提醒窗口只认领一次、清扫不重复处理）
- 新增 `tests/test_wish_service.py`（含**星期表反推**：由 `wish_weekly_limits` 与 slot_date = 开团日 + 天数 推出「周三与周日不可开团、周四 2 支、每周最多 6 次」；**名单可解析**：`admin_roster` 的批量输出真能被 `parse_batch_scores` 解出 N 条）
- 新增 `tests/test_wish_commands.py`（闸门/功能开关/群名单/状态阻断、`祈愿组队` 不接受队名、开团播报、换人全链路、`清空` 确认门、真发送通道在会话串未知时跳过）
- 新增 `tests/test_status_source.py`（`source` 列：插入时写入、续期不改写、老库补列后旧行为 NULL、批量查询也带来源）
- 新增 `tests/test_rename_status.py`（原地改名保期限/阻断/来源、目标名存在时合并、玩家与状态不存在、`split_rename_pair` 的六种输入）
- 改写受影响的既有用例：`test_status_blocks.py` 的老库升级用例现在按 `initialize()` 的迁移顺序补两列（`source` 是后加的，那个年代的老库两列都缺）；`test_migration_ledger.py`、`test_statuses.py` 不变

## [3.8.0] - 2026-09-24

### 破坏性变更
- **WebUI 的 `whitelist` 配置项废弃** — 诸神白名单现在只存在数据库里（由 `白名单` 指令与群同步写入）。删掉这一层的理由：只有部署者一个人会填，而部署者本身就是超管，纯属冗余。**配置里已填的条目不迁移、也不再生效**，升级后请用 `白名单 add <QQ> [信仰]` 重新录入，或让成员入群后走待审。schema 键已在本版**物理删除**（WebUI 不再显示）；旧配置里残留的值不再生效，启动时会打一条告警说明
- **群主/群管理员降为诸神级** — 此前 `_is_plugin_admin` 把 `config.admin_ids` 与 QQ 群角色混成一个判定，还被直接当作管理员闸门使用。后果是任何一个群的群主都能**全局**增删白名单、清空本群数据、重置全员积分。现在管理类闸门只认 `admin_ids`：
  - 失去：`白名单` 全部子命令、`同步白名单`、`天梯榜管理` 的 `重置/全部重置/清空/清除弃誓/迁移储物空间`
  - 保留（诸神级）：录入积分、批量录入、查询、天梯榜/觐见榜、`天梯榜管理 删除/改名`、道具与赠送，以及 8 条群管指令
- **入群不再自动授权，改为待审** — `auto_whitelist_group` 的入群者只进入待审名单（`白名单 待审` 可见），需「白名单 通过 <QQ> / 全部通过」确认；退群仍自动撤销。此前"谁能进这个群"就等于"谁能成为诸神"，而白名单是全局的，进一个群即全局生效。**`同步白名单` 同样只写待审**，不再直接授权——否则一条命令就能把全群封神，待审形同虚设

### 变更
- `白名单 add <QQ> <信仰>` 对已在名单的诸神按**改信仰**处理（此前回一句"已是诸神之一"，信仰被静默丢弃）
- `同步白名单` 改写**批量待审**：单事务、先读现有 id 再插差集（写锁在手，计数精确），取代逐人一次 commit、且靠 `IntegrityError→rollback` 探测已存在者；DB 异常时如实回复"未写入任何改动"。回复里提示需再用「白名单 全部通过」确认
- `白名单 全部通过` 新增护栏：待审**超过 5 人**时先返回名单预览并**不落库**，要发「白名单 全部通过 确认」才执行；两种情况都在回复里回显被通过的人（超过 20 个截断为"…等 N 人"）。阈值是 `PermissionService.BULK_APPROVE_CONFIRM_THRESHOLD`
- `白名单 remove` 改为连**待审记录一并清掉**（语义是"把这个 id 从我名单里拿掉"）。此前同一 id 若同时有 `user` 与 `pending` 两行，移除后还会留在待审列表里
- 待审写入加守卫：**已在名单里的 id 不再产生待审行**。`INSERT OR IGNORE` 的唯一键只管 `(entry_type, entry_id)`，挡不住"已是诸神的人入群又拿到一条 pending"，那种冗余行会让他出现在待审列表里等着被批准
- 新增 `同步白名单 清理`：移除**由群同步写入**（`added_by` 为 auto/sync）且已不在群里的条目（`user` 与待审都对账）。默认仍是"只增不删"；手动 `白名单 add` 的诸神即使不在群里也不会被清理。同步不再改变任何人的权限，因此只在清理真的删了人时才失效权限缓存
- `天梯榜帮助` 纳入群访问闸门，未启用的群不再泄漏帮助文本
- 权限缓存：加代数计数器，堵住"失效发生在 DB 查询途中、旧结论带新时间戳写回"（被移出白名单的人最多还能再通过 5 分钟）；加 `MAX_CACHE_SIZE = 1024` 上限（此前从未重复出现的 user 永不回收，自动同步群持续进人会让缓存单调增长）
- `白名单 add/remove` 改为按目标 id 失效缓存，不再清空全部
- `get_whitelist_with_faith` 在 SQL 层过滤 `entry_type = 'user'`，非授权条目不再可能被当成诸神展示
- 群访问名单跳过 `None`/空条目（`str(None) == "None"` 曾被当成一个真实群号留在名单里）
- 修正 `_conf_schema.json` 中 `group_access_list` 的 hint（原文称"留空不产生限制"，实际 whitelist 模式留空会拦下所有群），并补上 `items` 声明
- `admin_ids` 的 hint 如实说明它是唯一管理权限来源；`commands/query.py` 手写的权限判定收敛到 `_check_perm`
- **启动迁移改为记账式**（新表 `schema_migrations`）：`_migrate_item_names` 与 `migrate_player_items` 成功提交后落一条记账，之后的启动直接跳过。此前两者每次启动都全表读 `player_items` 并逐行查一次，启动成本随道具行数线性增长。迁移**失败不记账**，下次启动自动重试；`天梯榜管理 迁移储物空间` 改为 `force=True` 强制重跑，不受记账影响。`_migrate_items_grade_pk` 不进记账表——它只做 `PRAGMA` 判断（健康库上是 O(1)），而它的判断条件会随代码演进，用记账跳过反而危险
- 收回道具：一次命令只读**一次**道具表（此前循环内每件道具都重查一遍玩家的全部道具）；同一件道具在一次命令里出现两次时，第二次按扣减后的数量判定，命中并发扣走时重读一次再继续
- 6 处「吞掉异常且不留任何痕迹」的处理器改为记日志（取群名片、取群成员列表、取群成员信息、解析 @ 用户、注册文案渲染、权限配置读取、数据目录回退），**行为不变**，只是不再静默
- 名片解析的两处已知歧义（具体职业前缀截断名字「小丑鱼」→ 名字「鱼」、多个非关键词直接拼接「张三 小明 阿伟」→「张三小明阿伟」）补上注释与钉死用例，明确写成**既定行为**，避免后人反复当 bug 顺手改

### 新增
- **诸神赞美** — 白名单里**且有信仰**的诸神，发出命中自己信仰的祷词时，神明回一句「赞美【信仰】」。不计分、不写记录、不限频率；判定严格只认诸神名单（超管与群主/群管理员不在此列）。文案为插件自拟，原著无对应整句
- 待审名单复用 `whitelist.entry_type` 列承载 `'pending'`——所有授权路径都写死 `entry_type = 'user'`，待审天然不授权，**无需新表、无需迁移**
- DB：`get_pending_whitelist` / `approve_pending` / `approve_all_pending` / `reject_pending` / `add_pending` / `add_many_pending` / `remove_many_from_whitelist` / `remove_whitelist_entry_everywhere`

### 升级注意
- **需要动手**：WebUI 里 `whitelist` 配过人的话，他们升级后会立刻失去诸神权限，请先用 `白名单 add` 补回，或确认他们已在 DB 名单里
- 群主/群管理员若此前在替你管白名单或清空数据，升级后会收到权限拒绝，请把他们加进 `admin_ids`
- 配了 `auto_whitelist_group` 的话，升级后入群与「同步白名单」都只进待审，需要你去 `白名单 待审` 放行；这会积压，别让它成为没人看的队列
- 本版不动 `whitelist` 表结构，无迁移；`whitelist` 配置键已从 `_conf_schema.json` 物理删除。因 `cfg_get` 对 schema 外的键是"读到即原样返回"，残留旧值时启动告警仍会触发；WebUI 里这一项会消失
- **升级后首次启动**会把两条道具迁移再跑一遍（`schema_migrations` 表此前不存在，属幂等重跑，结果与数据无变化），此后启动不再全表扫描。代价要知道：**记账之后才出现的**旧格式数据不会被自动清理，需要 `天梯榜管理 迁移储物空间` 手动跑一次——该指令已改为强制重跑，不受记账影响

### 测试
- 新增 `tests/test_permission_boundary.py`（群角色权限边界、缓存竞态与上限、群访问名单脏条目、help 过闸门）与 `tests/test_whitelist_flow.py`（入群待审 → 审核 → 退群撤销全流程、待审写守卫、同步批量与清理、5 人阈值边界与预览不落库）
- 新增 `tests/test_god_praise.py`（诸神赞美：命中自己信仰才回、无信仰与超管不触发、闸门优先、16 条祷词非空且互不重复）
- 改写受影响的既有用例：`test_permission.py`、`test_review_fixes.py`、`test_config_access.py`、`test_config_hotreload.py`（配置热生效改用 `admin_ids` 验证）
- `test_config_access.py` 的「字面量键必须在 schema 里」守卫加例外表：`whitelist` 已从 schema 删除，但 `main.py` 仍要读它才能在启动时告警「配置里填的不会生效」；另加一条检查防止例外表留死条目
- 新增 `tests/test_silent_failures.py`：AST 守卫，禁止生产代码里出现「只捕获 `Exception` 且体里只有 `pass`」的处理器（窄异常与 `CancelledError` 不在范围），例外表必须写明理由且不许留死条目
- 新增 `tests/test_migration_ledger.py`：首次启动落账、已记账则跳过扫描、失败不落账并在下次启动重试、`force` 绕过记账
- `test_card_utils.py` 新增 `TestParsingAmbiguities`（四条，把上述两处歧义的现状钉死）、`test_items.py` 补收回道具的查询次数与同名重复请求用例
- 守卫测试均做过故障注入验证：记账失效时四条全红、去掉跳过判断时两条红、注入裸 `pass` 与删除键读取例外时守卫报红
- 总计 766 通过

## [3.7.10] - 2026-09-24

### 变更
- **文案全面对齐小说《诸神愚戏》原文用词**
  - 「找不到玩家」收敛成一种说法：`在本宇宙未寻找到（玩家名）`，取代此前散落在服务层与指令层的 5 种写法（`{name}未引起寰宇诸神的注意。`、`{name}不存在这个宇宙`、`未找到玩家: {name}`、`玩家 {name} 不存在`、`本宇宙未找到玩家: {name}`）
  - 去自造词：`命册` → `本宇宙`；赌局台词的 `N 息内` → `N 秒内`（原著用「秒」「片刻」计时，不用「息」）
  - 觐见之梯榜首新增「神选？」标记。原著里「神选」指觐见之梯中每个信仰排名第一的玩家，而玩家问出这句时本身就是疑问句（「你不会是【诞育】的【神选】吧？」），所以带问号
  - 「恩主」收窄到**只指玩家自己所奉的那位神**（信仰主题文案、祷词命中、连续天数提示、弃誓兜底）；泛指神明处仍是「神明 / 诸神」——系统播报、状态降下、诸神列表的信仰栏都不套这个词。用法集中在 `terms.py`，改词只改那里
  - 录入玩家成功时追加一行神明定称号 / 谕行（如「文明的序幕——【秩序】的规则里，从此有你一行。」），全部取自原著；原著没给出表述的沉默与欺诈不加这一行，不杜撰
  - 16 条 `oath_text_*` 默认值改用神明定称号重写；弃誓兜底文案改为「原有的恩主不再注视你」

### 移除
- **积分播报的「神明视角短句」整体下线** — 删掉 `SCORE_FLAVOR` / `GENERIC_SCORE_FLAVOR` 文案池、`pick_score_flavor`，以及配置项 `score_flavor_enabled` 与 `score_flavor_messages`。积分变更现在固定输出箭头体三行
- **「试炼刻痕」（里程碑提示）整套移除** — 删掉 `progress.detect_milestones` 与 `KIND_*`、`messages.MILESTONE_TEMPLATES`、`render_milestones`；`add_score` 为算名次多做的两次查询随之取消（每次录入少 2 次 SQL）。这套提示只挂在极少使用的单条 `录入积分` 上，实际结算几乎都走批量录入，而批量链路本就不输出这些行
- 数据库 history 的 `reason` 字段未改动（其中没有自造词）。注意**历史记录不回溯**：早先写入的记录里仍是旧文案

### 新增
- `terms.py` — 原文术语表，区分「原文用词」（带行号）与「插件自造」（明确标注，别当原文）；「恩主」的用法收敛成三个函数，改词只改这里
- `tests/test_copy_guard.py` — 文案守卫：生产代码里不许再出现已清退的写法（`命册` / `息内` / `不存在这个宇宙` / `寰宇诸神的注意`），「恩主」只许出现在白名单文件

### 升级注意
- 配置里遗留的 `score_flavor_enabled` / `score_flavor_messages` 可以直接删掉，插件已不再读取；建议升级后到 WebUI 确认这两项没有以"幽灵项"形式残留
- **本次改动不可逆**：分数短句与试炼刻痕两套文案已删除，想恢复只能回退代码。社群侧玩家会察觉积分回复少了一行，如有必要请提前说明

### 测试
- 重写 `tests/test_immersion.py`：移除里程碑与分数短句用例，补「恩主」与神明定称号的断言
- 新增：觐见榜「神选？」标记、注册回复的定称号行、`format_score_result` 不再有 `flavor` / `milestones` 入参、帮助文案跟随 `cmd_take_oath` / `cmd_abandon_oath` 配置、文案守卫
- 总计 692 通过（守卫测试加入前的统计）；版本 3.7.10（未推送）

## [3.7.9] - 2026-09-24

### 变更
- **内部清理（无用户可见变化）**
  - 删掉 `_pending_gifts_receive`：它从来只被写入、弹出，**没有任何读取点**，注释却写着"内存缓存减少查询"。待处理赠送本来就以数据库为权威（`_get_valid_pending_gift` 每次查库并带 `created_at` 判超时），所以这套缓存连同 `_forget_pending_gift_cache` 与调度器的 `on_gift_refunded` 接线一并移除。`LadderService.cleanup_expired_gifts` 的 `on_refunded` 钩子保留——它是给调用方的通用能力，仍有用例覆盖
  - 清掉未使用导入与重复导入：`main.py` 里 8 个已迁去 `commands/` 的消息常量、`message_formatter` / `qq_admin_handle` / `scheduler_service` 的多余符号，以及 `commands/*.py` 里只写不用的 typing 名；`qq_admin_handle._get_faith_message` 内与模块顶部重复的 faith_messages 导入也去掉（此前它把模块级导入整个遮住）

### 测试
- 总计 702 通过；版本 3.7.9（未推送）

## [3.7.8] - 2026-09-24

### 修复
- **备份失败被吞掉，当天却仍记为「已备份」** — `_backup_loop` 原本无条件把当天标记为已完成，而 `_do_backup` 只把异常写进日志。于是「自动备份开着」会安静地变成「今天没有备份」，并且整整一天不再重试。现在 `_do_backup` 返回成功与否，失败时不记日期（下个周期 10 分钟后自然重试）；未注入备份回调时也会留下 warning，而不是静默跳过
- **跨 await 的多步写入会被别的命令提交掉半步** — 共享一条 aiosqlite 连接时，它只串行化**单条**语句。`register_player`（玩家 → 职业/命途 → 具体信仰 → 积分历史 → QQ 绑定）与 `batch_add_scores`（批量录入）的多个 `await` 之间，别的命令可以插进来执行并 `commit()`，把半成品一起提交掉——文档里写的"原子提交""注册回滚"因此并不成立：QQ 绑定竞态时库里会留下一个没有 QQ 绑定的半成品玩家
  - `DatabaseManager` 新增可重入的串行化闸门：所有对外 async 方法（读也一样，单连接 SQLite 上没有"并发读"）统一排队；`transaction()` 作为多步写入的原子边界，正常退出统一提交、中途抛错整体回滚
  - `backup_to` 显式豁免：它用独立连接、耗时可观（VACUUM INTO），不该在每天备份时把指令一起卡住
  - `rollback()` 改用 `self._db.rollback()`，不再发裸 `"ROLLBACK"`

### 测试
- 新增 `tests/test_db_transaction.py`（6 例）：事务进行时其它写入必须排队、块内读与显式 `commit()` 可重入不死锁、抛错整体回滚、注册撞上 QQ 竞态必须撤销全部四步
- `tests/test_plugin_wiring.py` 增补 2 例：备份失败要能返回给调用方、失败后下个周期继续重试
- 三条新用例均做故障注入验证（分别去掉串行化、去掉回滚、改回无条件记日期，各自变红）
- 总计 702 通过；版本 3.7.8（未推送）

## [3.7.7] - 2026-09-24

### 修复
- **「录入玩家 @用户」把玩家名解析成了具体职业名** — 名片 `【混乱】子琅 渔夫 1000 100` 里的「渔夫」是**混乱·猎人的具体职业名**。诸神把它照抄进参数（`录入玩家 @子琅 混沌 渔夫 1000 100`）时，参数分类只认 6 命途与 6 基础职业，「渔夫」便落进"玩家名候选"；又因为显式名优先于名片解析，被 @ 的人被**静默录成一个名叫「渔夫」的玩家**（职业与命途由名片补全，各项校验还能全过）。现在参数分类会先认**具体职业名**（归一成基础职业，并带出信仰与命途）和**具体信仰名**（推出对应命途），职业/信仰/命途词一律不占玩家名槽位，只有真写了姓名时才会覆盖名片名。`录入玩家 @张三 繁荣 战士` 这类把信仰写进参数的用法同样不再把「繁荣」当名字
- **名片的【标签】里写命途会被整段丢弃** — `parse_card_info` 只接受 16 个具体信仰，`【生命】战士 张三` 解析不出命途（同一个词写在正文里本就能识别），录入时直接报"缺少必要参数: 命途"；现在标签是命途时同样补全命途
- **【标签】必须紧贴名片开头才生效** — 与 `extract_specific_faith`（全文搜索）结论不一致，`Lv.9【生命】战士 张三` 会漏掉标签；现在标签不在开头也会全文找一个，并把标签从待分类文本里摘掉（免得整段被当成玩家名）
- **取群名片失败被静默吞掉** — 此前只回一句「缺少必要参数」，看不出是机器人没有查看群成员的权限、还是对方已退群；现在记 warning 日志（含群号与 QQ），并在提示里直接说明原因、引导改为手填

### 说明
- 用法占位符对齐：`录入玩家` / `立誓` / `弃誓` 的「信仰」改为「命途」（代码校验的一直是 6 命途），并注明命途与职业也可写具体信仰名（如 `繁荣`）、具体职业名（如 `渔夫`）
- 已知取舍：若某人**真的**以具体职业名（如「渔夫」「园丁」）作为玩家名，就不能再靠裸参数录入了——这与基础职业名（如「战士」）一直以来的行为一致；写明姓名之外的词仍按名片补全

### 测试
- 新增 `tests/test_register_args.py`（6 例）：用报障名片跑到落库为止，锁定「参数里的具体职业名/信仰名不抢玩家名」「显式姓名仍优先」「无 @ 时行为不变」「取名片失败要说明原因」
- `tests/test_card_utils.py` 增补 4 例：命途标签、标签不在开头、报障名片 `【混乱】子琅 渔夫 1000 100` → 玩家名「子琅」、职业「猎人」
- 总计 694 通过；版本 3.7.7（未推送）

## [3.7.6] - 2026-09-13

### 新增
- **静默时段：夜里不开局** — 新增 `wager_quiet_start_hour` / `wager_quiet_end_hour`（默认 0 与 8，按本地/北京时间判断，与每日重置口径一致）。落在静默时段里的槽**直接跳过**（不顺延到早上）：夜里不打扰，之后白天的槽照常开。两端填相同值表示不静默、全天可开；也支持跨夜区间（如起点 23、终点 7）。全新安装在夜里同样会等到白天

### 说明
- 开局时刻本来就是「每天不一样」的：抖动的随机种子是「群号 + 绝对时间槽」，所以同一个钟点在每天会得到不同的延后（新增用例锁定：一周内至少出现两个不同时刻）。默认抖动 5 分钟，想让一天里更分散就把 `wager_jitter_minutes` 调大

### 测试
- `tests/test_wager.py` 扩充到 70 个用例：新增静默时段八例（默认窗口、终点 8 点不含、两端相同即关闭、跨夜区间、夜间槽跳过、白天槽照常、全新安装夜里等待、跨日延后不同）
- 已做故障注入验证（去掉静默判断后夜间用例失败）
- 总计 685 通过

## [3.7.5] - 2026-09-13

### 变更
- **开局时刻改为「UTC 时间轴对齐 + 随机抖动」** — 原来是相对计时（距上次开局满 N 分钟，重启还会从头算），现在按 UTC 时间等分：间隔 30 分钟 → 每小时的 :00 与 :30；间隔 60 → 整点；再叠加每群固定的随机延后 0~`wager_jitter_minutes` 分钟（默认 5，填 0 表示正好卡在等分点，超过间隔会收敛防止漂出本槽）。同一时间槽内只开一局；抖动以「群号 + 槽起点」为随机种子，重启后重新算也一样，不会出现「说好 8:03 变成 8:07」。全新安装（库中无记录）仍然立刻开第一场，方便先看到效果
- 附注：间隔为 30 / 60 分钟时，等分点在中国时区同样落在整点与半点（时区差是整小时），不需要额外换算

### 测试
- `tests/test_wager.py` 扩充到 62 个用例：新增「排期」八例（抖动确定性与取值范围、填 0 卡在等分点、抖动被收敛到间隔以内、同槽不开第二局、跨槽再开、槽内未到随机时刻不开、按小时对齐、全新安装立刻开局）
- 已做故障注入验证（去掉同槽守卫后 4 条用例失败）
- 总计 677 通过

## [3.7.4] - 2026-09-13

### 修复
- **只有结算、没有开局提示** — 会话标识未知时（插件刚重载、该群还没有任何消息）开局播报根本发不出去，但 60 秒后照样结算，群里就凭空冒出一条结算消息。现在**会话未知就不开局**：等收到过该群消息、能确定发送目标之后再开；跳过期间既不落库也不结算
- **间隔在重载后从头开始算** — 间隔计时器只在内存里，插件一重载/重启就清零，于是「刚重启就冒出一场赌局」、看起来间隔不对。现在用数据库 `god_wagers.started_at`（每次开局都会写）读回上次开局时间兜底，跨重启依旧按间隔走；全新安装（库里没有记录）仍然会立刻开第一场。同时把时钟从单调时钟改成 wall-clock，否则没法与数据库里的 UTC 时间戳相减

### 测试
- `tests/test_wager.py` 扩充到 53 个用例：新增「会话未知不开局」三例（跳过且不落库、跳过期间不产生孤儿结算、会话可用后恢复开局）与「间隔跨重启」四例（近期有记录则重启后不开、超过间隔则开、UTC 时间戳解析、无记录返回 None）
- 两处修复都做了故障注入验证（移除守卫后对应用例失败）
- 总计 668 通过

## [3.7.3] - 2026-09-13

### 修复
- **赌局播报仍然发不出去（v4 要 MessageChain，不是 list）** — 会话串修好后暴露出下一层：`context.send_message(umo, [Plain(...)])` 在 AstrBot v4 上抛 `'list' object has no attribute 'chain'`（框架内部要访问 `.chain`），消息依旧被丢弃。现在统一用 `wrap_message_chain()` 包成 `MessageChain(chain=[...])`；导入不到该类型时退回 list（旧版本直接收 list），不会因此报错。同样修正了赠送超时通知

### 测试
- `tests/test_plugin_wiring.py` 新增 MessageChain 包装用例（返回对象带 `.chain`、无框架时退回 list）与一条 AST 静态守卫（禁止给 `send_message` 直接传 list 字面量，文档字符串里的示例不算），并更新了发送通道断言；已用故障注入验证
- 测试替身补齐 `astrbot.core.message.message_event_result.MessageChain`
- 总计 661 通过

## [3.7.2] - 2026-09-13

### 修复
- **赌局开奖消息根本发不出去（v4 会话串格式）** — 定时任务这一路（自动开局与开奖的播报、赠送超时通知）此前手工拼 `group:<群号>`，而 AstrBot v4 的 `send_message` 需要完整会话串 `<平台>:GroupMessage:<群号>`（如 `xiaoyu:GroupMessage:821721918`），旧写法会抛 `不合法的 session 字符串: not enough values to unpack (expected 3, got 2)`，消息被直接丢弃——表现就是「赌局开了、到点了却没有开奖消息」。现在每次收到群消息时记下真实会话串，定时任务发送前取值；没见过该群消息时用已知平台前缀拼一个；连平台都不知道时跳过并写告警（宁可跳过也不发到错的地方）
- 附注：手动 `赌局` 的开局播报一直是正常的（它作为指令回复直接发出，不走这条通道），所以受影响的只有「自动开局」与「开奖」两处

### 测试
- `tests/test_plugin_wiring.py` 新增 6 个用例 + 1 条静态守卫：会话串的记录与解析、未知群用平台前缀推导、无平台时返回 None、事件缺该字段时容错、赌局发送使用完整会话串、会话未知时跳过；静态守卫禁止生产代码再出现 `group:<群号>` 写法。已用故障注入验证（改回旧写法后 3 条用例失败）
- 总计 658 通过

## [3.7.1] - 2026-09-13

### 新增
- **手动开局指令** — `赌局`（别名 `wager` / `神明赌局`），诸神/管理员发送即在当前群立刻抛下一场赌局。与自动开局的差别：不看 `wager_groups`、不等间隔；其余条件相同（总开关、群未被访问控制排除、当前无进行中的赌局——已有赌局时会提示还剩多少秒）。开局文案由指令本身回复，不再重复广播

### 修复
- **赌局绕开了群访问控制** — 赌局有自己的发送通道（不走指令闸门），此前只要某个群同时出现在 `wager_groups` 里，即使它在 `group_access_mode` 的黑名单/白名单之外也照样会被开局并播报，"该群完全静默"的约定形同虚设。现在：开新局前检查、进行中的赌局照常收尾但**不发消息**（否则配置一改，状态会永远卡在内存里），并在统一播报出口 `_wager_notify` 再兜一次
- **抽到「献上祷词」却没人能触发** — 若该群没开祷词（不在 `prayer_trigger_groups`）或祷词功能被关闭，开局要求"献上祷词"就注定无人入局，开奖只能走"无人入局"文案。现在只在能触发祷词的群里才会抽到这个动作

### 测试
- `tests/test_wager.py` 扩充到 46 个用例：新增「群访问控制」三例（被排除群不开局且不落库、进行中的赌局静默收尾、播报出口拦截）、「动作可用性」四例（没开祷词时只抽说话、开着时可抽祷词、功能开关关闭时退化、辅助方法判定）与「手动开局」七例（开局成功、重复开局提示余量、总开关关闭提示、无权限拒绝、被排除群静默、入局仍计数、不等间隔）
- 已用故障注入验证：移除静默判断后相关用例会失败
- 总计 651 通过

## [3.7.0] - 2026-09-13

### 文档
- **README 全面刷新**：
  - 开头简介与「功能特性」按主题重组（积分与榜单 / 玩家与道具 / 权限与管理 / 沉浸感 / 平台特性），把三批沉浸感改造集中成一节
  - 新增「沉浸感系统」章节，带实际输出示例（棋局记录、位阶徽记、神明的赌局）与各自的关闭方式
  - 配置表按用途拆成 6 张子表并**补全此前没列出的键**：`whitelist`、`admin_ids`、`auto_whitelist_group`、`player_name_max_length`、`score_history_retention_days`、`oath_text_*`（16 位信仰的弃誓文本）、12 个 `cmd_*`、`inventory_easter_egg_messages` 等
  - 修正过期说明：「检测玩家」的权限早已收归诸神/管理员（README 仍写着"所有人"），身份判定口径更正为"名片能对上数据库玩家名即可"
  - 版本列表修掉 `v3.6.8` 位置错乱的问题，只保留最近 8 个版本，更早的指向 `CHANGELOG.md`
- 版本号定为 **3.7.0**：本次仅文档改动，但把 3.6.11 起的结构性改造与三批沉浸感改造合并为一个功能版本发布（代码与 3.6.15 相同）

## [3.6.15] - 2026-09-13

### 新增（沉浸感 · 第三批：神明的赌局）

调性对标《诸神愚戏》："神明高坐，以凡人的挣扎为戏"。**默认关闭**，在 `wager_groups` 指定的群里按 `wager_interval_minutes` 抛出：

- **开局** — 随机从 16 位信仰中请一位现身，限时 `wager_duration_seconds`（默认 60 秒）；入局动作随机为「开口说话」或「献上祷词」（祷词要求真正命中祷词才算，闲聊不算）
- **开奖** — 从入局者里随机点一个，结果三分：**兑现** / **嘲弄** / **嘴上反悔**。默认只发文案并注明"不影响实际分数"；`wager_score_enabled` 打开后只有「兑现」会给赢家加 `wager_reward` 点觐见分。**神明可以在台词里骗人，但绝不会真的动你的分数或道具**
- **文案** — 16 位神明 × 5 档（开局/兑现/嘲弄/反悔/无人入局）共 80+ 句默认台词，全部由本插件撰写（非小说原文）；`wager_messages_announce` 等 5 个配置项可整体替换，按神明覆盖用 `wager_messages_announce_欺诈` 这种形式。兑现类文案强制包含 `{reward}` 占位符（有测试守住，曾漏配 15 条导致奖励不可见）
- **规模控制** — 每场赌局只产生两条群消息（开局 + 开奖）；参与者上限 200 人；记录写入 `god_wagers` / `god_wager_entries` 两张表（新建表，无需迁移）
- 调度器新增 5 秒一跳的 `_wager_loop`（只在内存里比时间戳，几乎不耗资源）

### 修复
- **属性存在性检查器漏检新 mixin** — 它只认 `*CommandsMixin` / `*SendMixin` 两个后缀，新增的 `ConfigMixin` / `GateMixin` / `WagerMixin` 一直不在覆盖范围内。现改为匹配所有 `*Mixin`（正是这次 wager 的惰性状态被漏掉才暴露出来）

### 测试
- 新增 `tests/test_wager.py`（31 个用例）：开关与群过滤、开局/间隔/再次开局、入局（动作不匹配、超时、重复、机器人自己）、四种开奖结果与默认不改分、开启改分时只有兑现给分、落库（含重复入局被主键拦住）、无数据库时链路照跑、文案池优先级与"每种神明都有全部五档"、以及兑现文案必含 `{reward}` 的文案不变量
- `tests/test_plugin_wiring.py` 新增赌局定时入口接线断言
- 总计 635 通过

## [3.6.14] - 2026-09-13

### 新增（沉浸感 · 第二批：位阶徽记）

- **位阶徽记** — 按登神之路分数分 9 阶，榜单（天梯榜/觐见榜）与玩家档案在名字前显示**纯符号徽记**，默认从低到高为 `☽ ☿ ♀ ♁ ♂ ♃ ♄ ♅ ☉`，**不出现任何中文阶名**
- 阈值与符号都是配置项：`tier_thresholds`（升序 9 个分数）与 `tier_marks`（9 个符号，也可换成 `✦Ⅰ…` 之类）。符号配得比阈值少时，多出的阶层沿用最后一个（不会因为少配一个就整个不显示）；配置写坏（非数字、乱序）时自动清洗排序并回落默认表
- 不改任何玩法数值：徽记只是把"你在第几层阶梯"变成一眼能看出的符号

### 测试
- `tests/test_immersion.py` 新增 13 个用例：默认符号阶梯、低于/高于阈值边界、自定义符号、符号少于阶级的兜底、坏配置回落、按玩家映射、榜单与档案的显示与"无徽记时保持原样"，以及走服务层的配置生效
- 总计 604 通过

## [3.6.13] - 2026-09-13

### 新增（沉浸感 · 第一批：棋局记录）

数字反馈本身保持原样（`+16 → 1016` 的箭头样式一眼能看出变化），沉浸感来自"多一行"：

- **神明视角的分数短句** — 积分变化时在数字上方加一句按信仰取的话（如「【秩序】把你向上推了一寸。」）。内置 16 信仰各 3 句 + 通用 3 句；`score_flavor_enabled` 可一键关掉，`score_flavor_messages`（或 `score_flavor_messages_信仰`）可整体替换
- **试炼刻痕（里程碑）** — 跨过整百、跨过整千、首次越过上榜门槛、名次上升时，在数字下方追加一行刻痕（「—— 第 12 道刻痕。」「—— 阶梯轰鸣：你已走过第 1 个千阶。」「—— 你越过了 1 个人。」）。判定只用新旧分数与名次，不写数据库；扣分不会产生刻痕
- **祷词大成功** — 随机值正好落在配置的分值区间上限时，走独立的「大成功」文案池（16 信仰各 2 句，`prayer_trigger_messages_crit` 可覆盖；缺池自动回落到成功池）；渎神时不会误用
- **连续祷词天数** — 祷词回复追加「连续 N 天」提示（2 / 7 / 30 天三档文案），数据取自已有的按天记录表；第一次触发不提示
- **状态来源文案** — 施加状态时写明来源与期限：状态名与某位信仰同名时写作「由【沉默】降下」，其余写「由神明的意志降下」，并给出到期日期（如「至 09月22日（3天）」）

### 测试
- 新增 `tests/test_immersion.py`（41 个用例）：刻痕判定（百位/千位/门槛只刻一次/扣分不产生刻痕/名次上升）、短句池优先级（按信仰 → 通用 → 内置 → 兜底）与开关、祷词大成功与缺池回退、连续天数（含断档与玩家/群隔离）、状态来源文案；并已用故障注入验证关闭短句后对应用例会失败
- 总计 591 通过

## [3.6.12] - 2026-09-13

### 变更
- **榜单缓存时长可配置并加长** — 由写死的 30 秒改为配置项 `leaderboard_cache_seconds`（默认 **120 秒**，填 `0` 表示不缓存、每次都查库）。插件内任何改分/录入/改名/改信仰/清除弃誓/重置都会显式失效对应群的缓存，所以加长只影响"没有任何写入时的兜底新鲜度"；改配置立即生效（不需要重载插件）
- **补上两处缓存失效** — 祷词触发时会从群名片回填职业（基础职业与具体职业），而榜单是显示职业的，此前回填后最多要等一个 TTL 才更新；现在回填后立即失效缓存

### 说明
- **"录入玩家"本身不经过任何缓存**，录入路径会立即失效该群榜单缓存。如果录入后觉得榜单没变化，先看分数门槛 `leaderboard_min_ladder_score`（默认 1100）：新玩家默认 1000 分，会被挡在榜外——想让他上榜把这个门槛调成 1000 或 0

## [3.6.11] - 2026-09-13

### 新增
- **模块级功能开关** — 五个开关（`feature_gift_enabled` 道具赠送、`feature_inventory_enabled` 储物空间、`feature_prayer_enabled` 祷词、`feature_scoreboard_enabled` 排行榜、`feature_qq_admin_enabled` 群管指令）默认全开；关闭后对应指令回复「「X」功能已被管理员关闭」。**诸神的录入/道具管理指令不受开关影响**，避免管理员把自己锁死
- **状态阻断** — 状态可以携带"禁止项"，这是状态系统第一个真实游戏效果（此前只展示）：
  - `添加状态 张三 沉默 2 阻断=祷词,赠送`（可写多个、逗号分隔；写「阻断=无」清除阻断）
  - 被禁止的指令回复「你正处于「沉默」状态，暂时无法祷词。」；玩家卡片显示「沉默: 剩余2天（禁止：祷词、赠送）」
  - 只认发送者自己的状态（QQ 绑定优先，回退名片识别）；**识别不到身份时放行**——用弱身份把人拦下来反而会成为新的骚扰手段
  - 只续期、不写阻断段时不改动原有阻断项（`添加状态 张三 沉默 3` 不会顺带清掉禁止项）
  - 数据库迁移：`player_statuses` 加 `block_actions` 列（老库启动时自动补列，可重复执行）
- **群级访问控制** — 新增 `group_access_mode`（`off` / `blacklist` / `whitelist`）与 `group_access_list`：未启用的群里插件**完全静默**——不回复任何内容，也不抢事件（让其他插件或 AI 正常处理该消息）。此前想限制服务范围只能多部署一个实例。模式写成无法识别的值、或名单为空时一律放行（宁可不生效，也不要因为配置笔误把功能全关掉）；改完立即生效，无需重载插件
- **统一的指令入口闸门** — 24 个指令实现体与 8 条群管指令全部改为先过闸门 `self._gate(event, action)`（群管那 8 条不经 mixin，由 main.py 注入的回调接入），白名单自动同步也会跳过未启用的群。配两条静态守卫：任何 `_*_impl` / `handle_*` 漏掉闸门、或把闸门放在第一条回复之后，测试都会失败（已用故障注入验证守卫有效）
- **配置热生效（改 WebUI 配置免重载）** — 两处此前必须重载插件才生效的缓存现在跟着配置走：
  - 祷词表与指令前缀集合：此前只在初始化时构建一次，改了 `prayer_text_*` 或 `cmd_*` 必须重载插件；现在每次祷词触发前按配置快照比对，变过就自动重建（比对成本 = 读 28 个键 + 比一次元组，相比后面的归一化与 DB 查询可忽略）
  - 权限判定缓存：TTL 仍是 300 秒，但缓存条目里带上了 `admin_ids` / `whitelist` 的快照，这两个键一变立即失效——此前把某人加进白名单后最长要等 5 分钟才生效（白名单增删路径的 `invalidate_cache()` 语义不变）

### 修复
- **帮助里的查询冷却显示的是假数字** — `format_help` 把 `query_cooldown_seconds` 写死成 600，而实际生效的是 schema 默认的 5 秒。现在帮助显示配置里的真实值（新增用例锁定，已验证改回旧写法会失败）

### 重构（内部，运行时行为不变）
- **配置读取集中到一处** — 新增 `plugin_config.py` 与 `ConfigMixin._cfg(key)`，60 处 `self.config.get(key, default)` 全部改为从 `_conf_schema.json` 取默认值与类型。此后默认值只写一份（改默认值不必再搜遍全仓），WebUI 写成 `null` 或字符串的脏值在读取层统一转换或回落默认值，业务代码不必各自写 `int()`/`str()` 兜底；也给后面的"配置热生效"留好了唯一入口

### 配置
- `_conf_schema.json` 全部配置项补齐 `hint` 中文提示（说明取值、边界与影响），WebUI 里鼠标悬停即可看到
- 补上 4 个此前"代码在用、schema 里没有"的隐藏配置项：`prayer_trigger_messages_positive` / `_negative` / `_neutral` / `_mismatch`（祷词文案池）。此前只能手改配置 JSON，现在 WebUI 可直接编辑

### 测试
- 新增 `tests/test_config_access.py`（21 个用例）：schema 完整性（每项都要有 description/hint/type）、默认值与类型转换、坏值回落、`null` 处理、可变默认值必须返回副本、动态键读穿；外加两条静态守卫——生产代码不得再出现裸 `config.get(`、所有字面量配置键必须存在于 schema
- 新增 `tests/test_config_hotreload.py`（12 个用例）：改配置后祷词表/指令前缀立即生效、配置没变时不重建、权限缓存随 `admin_ids`/`whitelist` 立即失效、无变化时仍命中缓存；已验证禁用重建逻辑后相关用例会失败
- 新增 `tests/test_group_access.py`（16 个用例）：off/黑名单/白名单判定、模式写错与名单留空的兜底、真实指令被拦时"零回复且不碰服务层"、以及三条闸门覆盖守卫（每个入口都要过闸门、闸门必须在首次产出之前、豁免名单不得扩大）
- `tests/test_plugin_wiring.py` 新增 `TestQQAdminGate`（4 个用例）：群管指令被静默拦截时不执行也不抢事件、带文案拦截时回复并停止事件、未注入回调时行为不变
- 新增 `tests/test_feature_toggles.py`（20 个用例）：每个开关关闭时给出对应提示、默认全开时行为不变、管理类指令不受开关影响、开关表与 schema/文案表一一对应，以及真实指令被关时"只回复提示、不碰服务层"
- 新增 `tests/test_status_blocks.py`（25 个用例）：阻断项解析（中文标签/动作 id/全角逗号/去重/「无」/无法识别）、存储语义（续期不清阻断、空串清除）、老库补列迁移与幂等、闸门生效与不误伤其它动作、过期状态不再阻断、身份识别不到时放行、卡片展示，以及指令层「阻断=…」的四种写法
- 总计 543 通过

## [3.6.10] - 2026-09-13

### 新增
- **天梯榜分数门槛** — 登神之路低于 `leaderboard_min_ladder_score`（默认 1100）的玩家不上榜。过滤放在 SQL 里执行：若先按 LIMIT 取人再在上层丢弃低分玩家，榜上人数会不足（低分者占掉名额）。缓存键带上门槛，WebUI 改完配置不会继续按旧门槛显示最多 30 秒。全员低于门槛时榜面直接说明原因，避免被当成数据丢失。`排行榜` 是 `天梯榜` 的别名，二者为同一张榜（觐见榜不受影响）
- **祷词分值区间可配置** — 原先写死"命中 -2~+2、渎神 -2~0"，现拆成四项：`prayer_score_min` / `prayer_score_max`（命中自己信仰）与 `prayer_blasphemy_score_min` / `prayer_blasphemy_score_max`（信仰不匹配/渎神），默认值与旧行为一致。上下限写反会自动对调；某一项值非法时只回落该项默认值，不会让祷词触发整体报错

### 测试
- 新增 14 个用例：门槛过滤、低分不占名额、空榜解释、缓存键含门槛、命令层传参，以及祷词区间解析（默认值、写反、坏值、字符串数字、两个区间互不影响）与"不得写死区间"的静态守卫

### 调整
- 录入玩家的回复版式：`职业: X | 信仰：命途 | 具体信仰` 拆成两行——`职业: X` 单独一行，`信仰：…` 另起一行；文案分支与回退分支统一（新增 2 条用例锁定版式）

## [3.6.9] - 2026-09-13

逐函数审查里最后几处小而明确的缺陷（前一批见 3.6.8）。

### 修复
- **立誓/弃誓失败也烧掉 600 秒冷却** — 冷却原先在参数格式校验通过后、调用服务层**之前**就占用，而服务层还会因"玩家不存在/命途非法"失败；写错名字就被罚站 10 分钟。现在只有执行成功才占用冷却
- **管理端 `重置`/`清除弃誓` 对已消失的玩家报成功** — `update_scores` / `clear_oathbreaker` 返回 `None`（玩家在查询与写入之间被删除）时返回值被丢弃，照样回"已重置/已清除"。现改为明确报失败
- **禁言时长会被后面出现的数字覆盖** — 解析时长的内层 `break` 只跳出内层循环，外层继续扫后续文本段，`禁言 60 @某人 我记得 30 秒` 会被解析成 30 秒。现在只取**第一个**数字词（新增 3 个用例，已验证改回旧写法会失败）
- **`批量查询` 重复名字产出重复卡片与重复"未找到"** — `批量查询 张三 张三 张三` 会渲染三张相同卡片；如今请求列表先按顺序去重
- **帮助里的"初始之位"硬编码** `登神之路 1000 · 觐见之梯 100` — 群主改过初始分后帮助与实际重置结果不一致，现读配置
- `_build_prayer_cache` 的 docstring 声称"配置变更时调用"，但 AstrBot 没有插件内的配置变更钩子；已改为"仅初始化时调用，改配置需重载插件"

### 测试
- 新增 14 个回归用例（总计 429 通过）

## [3.6.8] - 2026-09-13

逐函数审查(约 220 个函数)后的修复。高危项集中在 v3.6.3 之后新写的代码上——此前审查都把"已修过的部分"排除在外,反而使这部分成为唯一未经第三方审计的区域。

### 修复
- **插件静默降级(本次最严重)** — `initialize()` 访问的 `db_manager.purge_old_score_history` 在 3.6.3 的清理中被误删,该调用抛 `AttributeError`;AstrBot 吞掉异常后插件仍能响应指令,但**其后的代码全部不再执行**:调度器从未启动(每日备份、积分历史清理、过期状态清理、赠送超时清理与通知全停),白名单成员变动监听也没注册。已恢复该方法
- **道具等级被误删** — `remove_item` 把 `grade=None` 当作"不过滤等级",于是「收回道具 张三 铁剑」(全部收回)会删掉铁剑的**所有等级**却只报无等级那一行的数量。现 `grade=None` 一律指"无等级那一行";清空全部等级由「清除储物空间」承担;若同名还有其它等级,回复会明确提示
- **赠送优先扣掉最高等级** — `get_player_items` 按等级降序返回,而 `deduct_item` 取匹配到的首项,于是「赠送道具 Bob 铁剑」优先扣 `铁剑(A级)`。现统一选行规则:未指定等级 → 优先无等级行、否则取等级最低的一行(`deduct_item` 与 `take_items` 共用);「收回道具」的编号模式也会带上该行等级
- **表重建迁移可能毁掉库存表** — `_migrate_items_grade_pk` 原用 `executescript`(先隐式提交、且不把脚本包在事务里),在 DROP 与 RENAME 之间失败就会丢表,且下次启动因读不到源表而无法自愈。现改为事务内逐条执行,并新增恢复分支:从暂存表还原数据(含"主表已被重建为空表"这一最隐蔽的情形)
- **备份可能因他人持有事务而失败** — 改用独立连接 + `sqlite3` 在线备份 API。不再依赖"不在事务中"这一前提,也不在共享连接上 commit(后者会把多步处理器写了一半的中间状态落盘,破坏原子性)
- **`信仰：None | 繁荣`** — 命途被清空(弃誓不留新信仰)而具体信仰仍在时会渲染出 None。`format_faith_line` 现在容忍任一缺失;弃誓/立誓也会对账具体信仰

### 修复(既有缺陷,逐函数审查后续报)
- **群管指令对"白名单之外的插件管理员"必崩** — `_check_permission` 直接 `await` 同步方法 `_is_plugin_admin(event)` 的返回值,抛 `TypeError: object bool can't be used in 'await' expression`;只有白名单命中的账号能正常用群管指令。现三个注入回调都改为"可 await 就 await"的兼容写法,并补了同步/异步两种形态的测试
- **脏数据道具名清理会污染其它等级** — `_migrate_item_names` 的合并按名字匹配(无 grade 条件),会把无等级行与 A 级行改成同一个数量;现按 (名字, 等级) 定位、按 rowid 更新与删除
- **老库存在重复 QQ 标识时插件每次启动都失败** — 无条件 `CREATE UNIQUE INDEX idx_players_qq` 抛出的 `IntegrityError` 会逃出 `initialize()` 且无补救路径。现改为 fail-soft:记录重复行、跳过建索引、明确告警(QQ 唯一性暂不生效,清理重复后重启即可)
- **白名单配置的两个边界** — `faith: null`(WebUI 写 null)会让 `白名单 list` 与 `get_god_faith` 抛 `AttributeError`;不写 `type` 的条目会"权限生效但列表里看不到"(授权侧与展示侧判定不一致)。现统一归一化
- **`换绑QQ` 对不存在的玩家报"换绑成功"** — 补 rowcount 判定;并补上并发下的唯一冲突保护
- **`撤回` 两处越界** — 空消息链 `chain[0]` 抛 `IndexError` 且不执行 `stop_event`;历史消息缺 `sender` 字段会让 `asyncio.gather` 整体失败
- **批量录入的分数解析** — 文档承诺正负号与冒号可选,但正则强制要求,`【登神之路 16】` 被静默当成 0(若这是该玩家唯一的数据,整块还会被丢弃);另修正畸形的"获得道具"块跨行吞掉后续标签、把 `【登神之路+16` 当道具名的问题
- **道具名中间的 `*数字` 被当成数量** — `3*3矩阵` 会被改坏成 `3矩阵`。数量标记现只认"末尾"或"紧邻结尾等级括号之前"两种位置
- **`赐予道具` 接受空基础名** — 形如 `(B)` 的输入会落库成一个没有名字的道具

### 安全
- **`检测玩家` 的权限收归诸神/管理员**（原为所有人）— 该指令的身份解析会回退到群名片（弱身份，名片由玩家自行修改），并据此在未绑定时把调用者的 QQ 绑到名片对应的玩家记录上。开放给所有人时，任何人把名片改成他人名字发一次即可抢占对方绑定（随后可用「赠送道具」取走其库存）。现改为只有诸神/管理员可调用：弱身份路径只由受信用户触发，可疑绑定由诸神人工审核。身份判定口径保持「群名片能对上数据库里的玩家名即可」，不再叠加额外限制
- **祷词不再自动绑定 QQ** — 祷词可能通过"名片回退"识别身份,而群名片是玩家可自行修改的弱身份:在回退路径上自动绑定,任何人把名片改成他人名字发一次祷词,就能永久抢占对方的 QQ 绑定,之后可用「赠送道具」取走其库存。现保留名片回退(仍可得分),但绑定只发生在受控路径(录入玩家 @用户 / 绑定QQ / 权限已收归诸神的检测玩家);另加静态守卫测试防止回退。同时修掉补全路径的三处问题:空命途被写成 `""`、具体职业补全后未同步内存对象导致本次触发被丢弃、职业匹配遍历了未排序的表

### 新增(两条防线)
- `tests/test_plugin_wiring.py` — **插件启动冒烟测试**:用最小 astrbot 桩真实执行 `__init__` / `initialize` / `terminate`(建库 + 全部迁移 + 调度器启停),并断言各定时回调均已接上。已验证:把该方法改名后测试会**快速失败**并点名缺失属性(此前写法会挂住,已修正为 initialize 置于 try 内)
- `tests/test_symbol_refs.py` — **属性存在性静态检查**:纯 AST 对照 `self.<service>.<member>` 与服务类的方法/属性表,并带自检用例确保检查器本身有效
> 在此之前 379 个测试**没有一个导入 main.py**,两条静态检查也都不看属性访问——这正是上面第一条能溜过数个版本的原因。

### 优化
- 每日任务新增每日表清理:`gift_daily_accepts` / `prayer_daily_hits` 此前从未清理(每次接受道具一行、每人每天一行,随使用无限增长),现按 90 天清理

### 优化(一致性收尾)
- **排行榜缓存失效补全** — `register_player`、`set_class`、`set_faith`、`abandon_oath` 以及管理端的 `改名`/`清除弃誓` 此前都不失效缓存，榜单最多陈旧 30 秒（会显示旧名字、旧职业、旧信仰、旧弃誓标记）
- **备份三处守卫** — 保留天数钳到 ≥1（配 0 会把刚生成的备份也删掉）；依据已有备份文件名判断"当天是否已备份"（当天重启不再重跑）；同一秒的两次备份加计数区分文件名（此前同名会被静默覆盖）
- **`record_prayer_hit` / `record_gift_accept` 异常收窄** — 只把唯一约束冲突当作"并发重复"，其它错误改为 log + rollback，不再一律静默当成重复
- **`add_to_whitelist` 冲突分支补 rollback**（与 `save_pending_gift` / `set_player_qq` 一致）
- `is_admin` 容忍 `admin_ids` 被配成字符串（此前会被逐字符拆开，`"1"` 反而成了管理员）
- 清理 `main.py` 中 7 个已无引用的导入；移除 `_resolve_target_or_self` 里不可达的 else 分支（顺带省掉一次重复的权限查询）
- `_resolve_self_player_lenient` 的文档改为准确描述并加醒目告警：它的名片回退是**弱身份**，调用方不得据此做持久化操作（绑定 QQ 只能发生在强身份路径）

### 新增(CI)
- `.github/workflows/tests.yml` — 在推送与 PR 时跑全量测试（Python 3.10 / 3.12）。这是唯一能长期生效的闸门；测试无需安装 AstrBot（依赖由测试内的桩模块提供）。不需要的话删掉该文件即可

### 测试
- 全量 416 测试通过(新增:启动冒烟 3 个、属性检查 4 个、迁移可回滚/恢复 3 个、选行规则 4 个、
  群管回调兼容 5 个、白名单边界 3 个、换绑 2 个、撤回边界 2 个、批量解析 4 个、数量标记 2 个)

## [3.6.7] - 2026-09-13 - 2026-09-13

### 重构
- **名片解析抽成可测试的纯函数** — `_parse_card_info` / `_extract_card_words` / `_extract_specific_faith` 的实现移入新的 `card_utils.py`（`parse_card_info(card, sorted_specific_classes)` 等），`main.py` 只留三个转发方法。这段逻辑此前只能通过整插件实例调用，测试根本碰不到，而它恰好最容易出错：三种信仰来源的优先级、具体职业名的前缀匹配（`酋长1218`）、非关键词与玩家名的划分。`main.py` 再降 110 行（867 → 757）

### 测试
- 新增 `tests/test_card_utils.py`：20 个用例覆盖标签信仰、具体职业（含数字后缀与名字后缀）、正文信仰词、命途词、标签优先于正文、16 个信仰全覆盖等
- 另做**差分验证**：从上一个提交取出旧实现，与新的 `card_utils` 对 26 张样例名片逐张比对，**0 处差异**，确认抽取未改变解析行为
- 全量 365 测试通过

## [3.6.6] - 2026-09-13 - 2026-09-13

### 重构
- **指令实现体全部按职责拆分出去**，`main.py` 从 2345 行降到 **866 行**（-63%）。25 个 handler 的实现体移入 `commands/` 下 7 个新 mixin：

  | 模块 | 职责 | 含指令 |
  |---|---|---|
  | `scoreboard.py` | 排行榜展示 | 天梯榜、觐见榜 |
  | `score.py` | 积分录入 | 录入积分、批量录入 |
  | `player.py` | 玩家档案与身份 | 录入玩家、检测玩家、绑定QQ、换绑QQ、设置职业、立誓、弃誓 |
  | `inventory.py` | 储物空间与状态 | 赐予道具、收回道具、清除储物空间、添加/移除/清除状态 |
  | `gift.py` | 道具赠送 | 赠送道具、接受道具、拒绝道具 |
  | `admin.py` | 管理 | 天梯榜管理、白名单、同步白名单、天梯榜帮助、白名单自动同步事件 |
  | `prayer.py` | 祷词触发 | 祷词监听 |

  （`query.py`、`shared.py` 为上一版已拆出的查询与通用发送）

- 约定不变：`@filter.command` / `@filter.regex` 装饰器与指令注册**全部留在 main.py**，mixin 里只有实现体。原因写在 `commands/__init__.py`：AstrBot 只扫描插件类自身的方法注册指令，装饰器放进 mixin 会被沿 MRO 重复扫到，导致重复注册并中断后续指令的注册
- 只被单个模块使用的辅助方法随模块搬走（如 `_get_valid_pending_gift` → gift、`_build_prayer_cache` → prayer）；跨模块共用的基础设施（身份识别、名片解析、配置读取）仍留在 main.py，由实现体通过 `self.` 调用
- 新增 `text_utils.py`：CQ 码 / @ 提及 / 名片标签等正则集中一处（此前散落在 main.py 顶部、多个模块都要引用），并提供共用的 `strip_mentions()`

### 修复
- **`录入玩家` 里一段引用已删变量的死代码** — 计算 `member_id` / `target_card` 的代码块在上次删除「对应群名片」提示时漏下，而它依赖的 `target_member` 已不存在，走到该分支会抛 `NameError`。拆分后由静态扫描发现并删除

### 测试
- 全量 345 测试通过
- 另做三项结构性验证：桩模块导入确认 34 个指令零重复、每个委托都指向真实存在的实现体、未定义名扫描无发现

## [3.6.5] - 2026-09-13 - 2026-09-13

### 优化
- **「录入玩家」回复精简** — 按需求去掉「（对应群名片：X，QQ: Y）」与「已自动绑定你的 QQ，后续可使用需鉴权的指令」两句；顺带省掉一次为拼这行而做的群成员列表查询
- **回复中的「命途」改为「信仰」，并带上具体信仰** — 形如 `职业: 战士 | 信仰：生命 | 繁荣`（只给命途时省略后者）。渲染复用玩家卡片同一函数 `format_faith_line`，避免两处各写一份再漂移

### 修复
- **具体信仰此前被解析出来就丢弃** — 名片解析只保留命途（如"生命"），具体信仰（如"繁荣"）虽然从标签里读到了却没有返回，也没落库，导致查询/文案/主题消息都用不上。现 `_parse_card_info` 一并返回并写入玩家记录（与职业、命途同一事务）
- **按信仰抽取的注册文案从未生效** — `FAITH_MESSAGES` 的键是 16 个具体信仰，而代码用 6 个命途去查，永远查不到、只能退回通用文案（CHANGELOG 3.5.0 的「录入玩家仪式化」实际没兑现）。现按具体信仰取
- **名片解析漏用"单独出现的具体信仰词"** — 这类词原先把从玩家名里剔除后就丢掉，现在会用来补全信仰与命途
- **`set_player_specific_faith` 可能清空命途** — 传入无法识别的信仰时 `FAITH_TO_PATH.get()` 返回 None，会把已有命途写成 NULL；现只在映射存在时才覆盖命途

### 测试
- 新增 `TestRegisterReply` / `TestFaithLineFormatter`：覆盖信仰行渲染、具体信仰落库、按信仰取文案、以及"回复不再承诺取消录入"
- **全量 345 测试通过**

## [3.6.4] - 2026-09-13 - 2026-09-13

### 修复
- **`录入玩家` 使用 @ 时丢失注册结果** — @ 分支把 `register_player` 返回的结果整条丢弃，只回一句提醒，导致用 @ 录入时看不到录入的玩家名、职业、命途、**刚录入的两个分数**以及信仰专属文案（同一句结果在非 @ 路径是正常发出的）。现两条路径回复统一：结果始终给出，@ 仅作为前置提及并说明已自动绑定 QQ
- **去掉一句不存在的后果** — @ 路径的提醒原文为「请及时进行谕行（或说出祷词），否则将取消录入」，但"祷词确认后取消录入"的机制在早前版本已移除（全仓库已无任何实现），该承诺属于历史残留，现改为「已自动绑定你的 QQ，后续可使用需鉴权的指令」

### 测试
- 全量 337 测试通过

## [3.6.3] - 2026-09-13 - 2026-09-13

全量代码审查后的一轮修复。多数问题此前不会立刻显形（需要并发、负数量、跨 8 小时时区边界或特定数据状态才触发），但都属于会静默损坏数据或让功能形同虚设的类型。

### 修复
- **负数量道具可凭空造道具** — `赠送道具 Bob 铁剑 -1` 之前会让发送方库存 +1（SQL 里减法变加法）。现在解析层与服务层都要求数量为正整数
- **批量查询带状态的玩家必定崩溃** — `get_statuses_for_players` 用 naive 时间减 aware 时间，抛 `TypeError`，用户得不到任何回复
- **状态提前 8 小时消失/被清理** — `expire_at` 以 UTC 写入，但查询与清理用 UTC+8 比较。现全链路统一 UTC
- **过期状态清理从未落盘** — `purge_expired_statuses` 没有 commit，调度器直接调用它；连接关闭后删除丢失，还会被任意一次 rollback 连带撤销
- **并发注册/绑定会抛异常** — 同一玩家名并发录入、同一 QQ 并发绑定都会抛 `IntegrityError` 而不是返回失败；QQ 唯一索引此前只在新增列时创建，半应用状态的库会永久失去唯一性约束
- **「注册回滚」是空操作** — 各子操作自带 commit，QQ 绑定失败时玩家/职业/积分历史其实已经落库，提示却声称已回滚。现四步共用一个事务
- **改名并发可产生同名玩家** — `player_name` 无唯一约束，「先查重名再 UPDATE」在并发下会双双重名成功；现把重名判断并入 UPDATE 语句
- **批量录入的事务失败被误报** — 回滚时返回的名单会被展示成「以下玩家不存在，已跳过」，掩盖真实数据库错误
- **赠送道具静默丢弃其余道具** — `赠送道具 Bob 铁剑 2 药水 3` 只处理第一件，玩家以为都送出了
- **数据库错误静默** — `get_message` / `INPUT_ERRORS` 两处未定义名会让查询、绑定、设职业、赐予道具在特定分支抛 `NameError`
- **删除玩家/清空群数据残留附属记录** — `pending_gifts` / `prayer_daily_hits` / `gift_daily_accepts` 未清理，同名玩家重新录入后会继承旧的待领取赠送与当日配额
- **同名不同等级的道具无法共存（数据保真）** — `player_items` 主键不含 `grade`，给已持有「铁剑」的玩家发「铁剑（A级）」会把等级并进无等级那一行后丢弃，之后按等级赠送/扣除都报"没有道具"。现将主键改为 `(group, player, name, grade)` 并自动迁移旧库，等级按行独立保存；`收回道具` 也改为按等级精确匹配（未写等级时优先收回无等级那一行）
- **备份可能是撕裂副本** — 备份从 `shutil.copy2` 直接拷活动中的 `.db` 文件改为 SQLite `VACUUM INTO` 一致性快照（旧方式不拷 `-journal`，可能拿到含未提交数据的副本）
- 其他：`白名单 add` 未校验信仰、`换绑QQ` 接受非数字、`@全体成员` 被当成真实 QQ 写入绑定、储物空间彩蛋配置为空列表时 `IndexError`、`检测玩家` 绑定失败仍报成功

### 优化
- **调度器的「每日」任务实际每小时执行一次** — 等于每天产生 24 份备份并把清理跑 24 遍。现按北京日期判断，启动后执行一次
- **冷却在参数校验前被占用** — 批量录入/立誓/弃誓写错格式会白烧 600 秒冷却，改对了也发不出去
- **重置单个玩家/改名/改职业后排行榜缓存未失效** — 榜单最长 30 秒内仍显示旧数据
- **弃誓文案的配置键错位** — 代码拼的是 `oath_text_<命途>`（6 个），而配置里是 `oath_text_<具体信仰>`（16 个），导致 16 个键全部失效、永远用兜底文案
- **诸神信仰未取配置值** — 只在 WebUI 配置里填了信仰的诸神，执行群管操作时退回通用文案
- **权限与冷却提示统一** — 此前存在「唯执棋者」「区区凡人」等 3 种写法，现全部走 `messages.py`
- **群管批量操作的结果汇报** — 解禁失败此前完全静默（无法区分「已解禁」和「没执行」）；禁言/踢人部分成功时只报错误
- `upsert_player` 的返回值此前因「名字是否变化」而来自不同来源（新建时返回分数为 0 的临时对象，改名时返回数据库真值）
- 自动白名单监听注册失败改为显式告警（此前静默跳过，表现为功能看似开启却从不同步）
- 自动补全信仰后同步内存对象，祷词首次触发不再被丢弃
- 批量录入单个道具数量上限 9999（`*2000000` 展开会占用数十 MB 内存）
- **破坏性操作增加二次确认** — `天梯榜管理 清空` / `全部重置` 需在参数后加「确认」，避免一条误发删光全群数据
- **参数中的 @ 提及文本统一剥离** — aiocqhttp 会把 `"@昵称(QQ)"` 拼进 message_str，此前 `录入积分 @张三 100 50` 这类写法会把目标解析成 `"@张三(12345)"` 而报玩家不存在
- **并列成绩的名次口径统一** — 批量查询此前用序号排名，同一成绩会排出不同名次，与单查的"严格领先计数"不一致
- **旧数据迁移补齐单一 `*N` 后缀** — `糖果*3` 这类名字此前不会被清理（文档却写着会），现与 `护身符（C级）*2`、`礼盒*2*3` 一并处理；两份重复的迁移实现合并为一份

### 重构
- 道具参数解析抽为 `item_utils.parse_item_args`（纯函数，不依赖 AstrBot，测试可直接调用）；`main.py` 仅转发
- 删除无调用者的 `should_trigger_now`、`get_nickname` 与未使用的 `ban_time` 参数
- `active_groups` 表及 `register_active_group` / `get_active_groups`（无任何生产调用）
- `format_whitelist`（仅测试使用，生产走 `format_whitelist_combined`）
- `db_manager.backup_database` / `cleanup_old_backups`（与调度器实现重复）
- 补齐 23 处缺失的函数 docstring

### 文档
- README 修正 12 处与代码不符的描述：天梯榜/觐见榜实际需要诸神权限、`帮助` 别名并不存在、`检测玩家` 不支持 @、赠送道具走 QQ 绑定而非名片识别、`query_cooldown_seconds` 默认 5（原写 600）、`backup_retention_days` 默认 7（原写 30）等
- 配置 schema 说明补充：`cmd_*` 仅影响帮助文案与祷词前缀过滤（不能真正改名）、`oath_text_*` 按具体信仰匹配

### 测试
- 新增 `tests/test_review_fixes.py`：23 个用例覆盖时区、并发、负数量、级联删除、改名冲突、
  按等级独立库存与旧库主键迁移
- **全量 337 测试通过**

## [3.6.2] - 2026-09-13

### 修复
- **道具数量标记后接等级括号时解析失败** — 道具解析此前有三份各自独立的实现，且都要求 `*数量` 必须位于整段末尾：
  - 「批量录入」中 `测试*10（b）` 会被当成名字叫 `测试*10（b）` 的无等级道具（数量 1）
  - 「赐予道具」「赠送道具」中 `道具*数量` 写法整段被当作名字，`测试（b）*10` 同样变成名为 `测试（b）*10` 的道具

  现将数量解析统一为 `item_utils.extract_item_quantity`，三处共用，数量标记位置不再受限：
  - `测试*10（b）` 与 `测试（b）*10` 均解析为 **10 个 B 级「测试」**
  - 同时识别本插件展示格式的乘号，`共生噬刃×3（C级）` 可直接复制粘贴
- `收回道具` 的道具名模式同样改用统一解析（此前 `*数量` 也必须写在末尾，写在等级前会解析失败）

### 优化
- 道具数量解析由三份实现（批量录入 / 赐予道具与赠送道具 / 收回道具）合并为一份，避免继续各自漂移
- 指令用法文案与 README 补充 `道具*数量`、`道具×数量` 写法说明

### 测试
- 新增 `extract_item_quantity` 单元测试，覆盖数量标记在等级前后、`*` 与 `×`、非数字 `*` 等情形
- 新增批量录入两种数量位置的端到端测试，并断言最终落库为 (测试, B, 10)
- 全量 318 测试通过

## [3.6.1] - 2026-09-13

### 修复
- **赠送道具完全不可用** — 此前走的是旧实现，同时存在 `parse_item_full_name` 未导入（NameError）与 `deduct_item` 参数错位两处缺陷，任何赠送都会失败；已改用此前被误粘进 `拒绝道具` 里的新实现，两处缺陷一并消失
- **赠送内存缓存泄漏** — 超时退款由调度器完成时没有清理内存缓存，该接收方此后在整个运行期间都收不到新的赠送；现由调度器退款回调清理，且判定改以数据库为准
- **并发下道具可被复制或丢失** — 道具扣减原本是「读-改-写」，并发赠送同一件道具时库存 1 能被成功送出 2 次；待处理赠送原本用 `INSERT OR REPLACE` 保存，第二笔会覆盖第一笔、而第一笔道具已扣除。现改为单条原子扣减 + 拒绝覆盖写入（写入失败自动退还发送方）
- **接受/拒收赠送可能重复发放** — 改为「先认领（删除记录）再发放」，并发下只有一方能成功
- **每日接受次数上限可被绕过** — `record_gift_accept` 的返回值此前被忽略，写库失败也照常发放
- **群主/管理员可能被误禁言或误踢** — 群成员信息接口失败时角色默认按普通成员处理，导致保护失效；现改为取不到角色就跳过该目标
- **`白名单 list` / `view` 不显示运行时添加的诸神** — 此前只显示 WebUI 配置项，用 `白名单 add` 添加的人一个都不显示（但权限实际已生效），还会提示"列表为空、可通过指令 add 添加"；现分「WebUI 配置」「运行时添加」两段显示并给出各自数量
- **`白名单 setfaith` 拒绝了所有合法信仰** — 校验误用 6 命途（`VALID_PATHS`）而非 16 具体信仰（`VALID_FAITHS`），导致 README 所写的 `白名单 setfaith <QQ> 湮灭` 这类用法全部被判为"无效信仰"；而信仰主题文案（`FAITH_MESSAGES`）恰恰只按 16 具体信仰索引，3.6.0 条目中记录的正是这一项修复，但代码里并未生效
- **数据库迁移中的未保护导入** — `db_manager` 的 `faith` 列迁移分支内重复执行了一次无兜底的 `from astrbot.api import logger`，使无 AstrBot 环境下所有测试在初始化阶段报错。**3.6.0 条目中记为"162 个 Windows 文件锁定错误"的其实是这一项**，与文件锁无关

### 新增
- **祷词真实改分开关** — 新增配置项 `prayer_score_enabled`（默认关闭）。关闭时保持原行为（展示随机结果并提示"本次结果暂时不会影响实际分数"）；开启后按匹配 -2~+2、渎神 -2~0 真实计入觐见分，回复中不再出现该提示

### 重构
- **指令实现体按职责拆分（试点）** — 新建 `commands/` 包，把 `查询` 与 `查询储物空间` 的实现体移入 `commands/query.py` 的 `QueryCommandsMixin`，`main.py` 只保留 `@filter.command` 装饰器与一行委托。装饰器一律不进 mixin：AstrBot 只扫描插件类自身的方法注册指令，把装饰器放进 mixin 会被沿 MRO 重复扫到，导致重复注册并中断后续指令的注册（表现为部分指令回复两次、另一些完全不触发）
- **合并转发逻辑收口** — 天梯榜 / 觐见榜 / 储物空间三处逐字重复的 `send_group_forward_msg` 逻辑合并为 `commands/shared.py` 的 `SharedSendMixin._send_forward_text`

### 优化
- **道具扣减改为单条原子 SQL** — 顺带修正 `收回道具` 超出持有数量时谎报收回数量的问题（此前会报"已收回 N 个"而实际只清空了库存）
- 删除重复定义：`ladder_service.take_items`（两份逐字相同）、`qq_admin_handle._check_permission`（两份逐字相同）、`ladder_service` 的重复 import
- 删除 `main.py` 中重复的 `cmd_accept_gift` / `cmd_reject_gift` 死代码共 225 行（含误粘进 `拒绝道具` 的赠送实现）
- 删除 `main.py` 中 5 个无引用的导入与 1 个无引用正则
- **诸神代收道具不再受每日上限约束** — 此前诸神用「接受道具」代收同样受 `gift_daily_accept_limit` 限制，且会占用接收方的当日配额（导致接收方自己反而无法再接受）；现诸神代收既不受上限约束，也不计入接收方配额
- 版本号与 `metadata.yaml` 对齐（此前 `@register` 写 2.1.0、metadata 写 v3.6.0），插件描述同步移除已删除的功能

### 移除
- **图片输出子系统**（早已无任何调用）：`image_renderer.py`、`templates/*.html`、`fonts/`、`Pillow` 依赖、`output_mode` / `image_quality` / `image_format` 配置项、`group_settings` 表及其访问函数
- `ladder_service` 中无调用者的 `get_leaderboard_players` / `get_pilgrimage_leaderboard_players` / `get_effective_output_mode` / `get_player_card_text`

### 测试
- 新增 `commands/` 包约定守卫测试：禁止在 mixin 中出现 `filter.` 装饰器、mixin 方法必须以 `_` 开头，防止重新引入指令重复注册
- 新增赠送并发语义测试：待处理赠送拒绝覆盖写入、删除的认领语义、超时退款幂等且回调清理缓存、并发扣减只有一个成功
- 新增祷词开关文案测试
- **全量 306 测试通过**（此前因上述未保护导入，实际为 151 通过 / 162 报错）

## [3.6.0] - 2026-08-29

### 新增
- **统一文案库** — 新建 `messages.py` 和 `faith_messages.py`，所有消息集中管理，支持变量替换和随机选取
- **信仰专属随机文案** — 16 信仰 × 6 种操作（禁言/踢人/赐予/收回/录入/清除弃誓），每种操作多条随机文案
- **诸神操作显示信仰身份** — 白名单支持 `faith` 字段，有信仰的诸神执行操作时显示对应神明文案（如"【沉默】降下神赐"）
- **白名单神明系统** — `白名单 add <QQ> [信仰]` / `setfaith` / `removefaith` / `view`，白名单绑定具体信仰
- **群管操作信仰主题化** — 禁言/踢人成功显示信仰专属文案，非静默
- **储物空间查询彩蛋** — 可配置概率触发趣味消息（如"【湮灭】令使路过你的储物空间..."），不影响实际道具
- **检测玩家支持 @** — 诸神可通过 `检测玩家 @用户` 查看他人绑定状态

### 优化
- **权限消息统一** — 所有权限拒绝统一为"此等权柄，唯诸神方可执掌。"
- **玩家不存在消息统一** — 统一为"{name}未引起寰宇诸神的注意。"
- **道具显示格式** — `*` 改为 `×`，去掉 `|`，如"共生噬刃×3（C级）"
- **赠送道具格式** — `道具 数量` 分隔（不再是 `道具*数量`），如"赐予道具 张三 铁剑 2 生命药水 3"
- **玩家档案格式** — `信仰：命途 | 具体信仰`，排名改为括号显示
- **排行榜简化** — 只显示命途，不显示具体信仰
- **批量录入文案** — "结算完成，X 人积分已变更"
- **帮助文案去"棋子/棋局"** — 改为"录入新信徒"等信仰主题表述
- **名片解析优化** — 过滤职业词，只保留玩家名用于匹配

### 修复
- **封神之路解析** — 兼容"封神之路"/"登神指路"等变体名称
- **配置 schema** — `inventory_easter_egg` 改为平铺配置项，避免 object.items 导致安装失败
- **白名单信仰字段** — `setfaith` 验证使用 `VALID_FAITHS`（16具体信仰）而非 `VALID_PATHS`（6命途）

### 测试
- 全量 313 测试通过（162 个 Windows 文件锁定错误不影响逻辑）

## [3.5.0] - 2026-08-28

### 新增
- **检测玩家指令** — `检测玩家`（别名：`检测`、`check`），检查当前玩家的绑定状态，未绑定时自动绑定 QQ
- **名片识别自动绑定 QQ** — 玩家首次使用查询/储物空间等指令时，通过名片识别到身份后自动绑定 QQ，后续可直接使用鉴权指令
- **祷词按 16 信仰配置** — 祷词和触发文案从命途级细化到信仰级，每个具体信仰有独立的祷词列表和回复文案
- **玩家具体信仰存储** — DB 新增 `specific_faith` 字段，解析名片 `【】` 时自动保存
- **录入玩家仪式化** — 新玩家录入显示「踏入棋局」仪式化回复
- **沉浸感优化** — 排行榜标题（凡人的阶梯/神明的凝视）、玩家卡片/储物空间/UI 分隔符沉浸化
- **权限/排行榜缓存** — 权限检查缓存 5 分钟、排行榜缓存 30 秒
- **祷词随机打分** — 匹配随机 -2~+2，渎神随机 -2~0，随机到 0 显示宽宏大量

### 优化
- **正则预编译** — CQ 码清理、名片解析等高频正则预编译
- **批量查询支持** — `查询` 指令支持多个玩家名批量查询
- **信仰/命途区分** — 代码层明确区分 6 命途与 16 信仰，配置、缓存、匹配全部按具体信仰

### 移除
- **移除 QQ 迁移代码** — `_maybe_trigger_qq_migration` / `_do_auto_migrate_qq` / `list_unbound_players`，功能已由自动绑定替代
- **移除组白名单支持** — 白名单仅保留 user 类型，简化指令语法

### 修复
- **缓存失效不完整** — 白名单变更/积分重置/删除玩家后立即失效对应缓存
- **时区问题** — 每日重置使用北京时间（UTC+8）而非 UTC

### 测试
- 全量 313 测试通过

## [3.4.0] - 2026-08-26

### 变更
- **每日接受道具上限可配置** — 新增 `gift_daily_accept_limit` 配置项（默认 1，0 为不限制）
  - `gift_daily_accepts` 表改为自增主键（支持多条记录/天）
  - `has_gift_accept_today` / `count_gift_accepts_today` 方法配合可配置限制

### 移除
- **移除每日推送功能** — 该功能因 AstrBot `send_to_group` API 格式问题从未正常工作
  - 移除 `scheduler_service.py` 中 `_daily_push_loop` / `_do_daily_push` / `_push_text_mode` / `_push_image_mode` / `_get_effective_output_mode`
  - 移除 `_conf_schema.json` 中 `daily_push_time` / `daily_push_enabled` / `daily_push_groups`
  - 移除 `message_formatter.py` 中 `format_daily_push_*` 函数和 `get_output_mode` 函数
  - 移除 `main.py` 中 `SchedulerService` 构造函数的每日推送参数
  - 移除 README 中每日推送相关文档和配置表条目

### 测试
- 全量 304 测试通过

## [3.3.0] - 2026-08-24

### 新增
- **每日接受道具限制** — 每位玩家每天只能接受一次道具（防止刷道具）
  - 新增 `gift_daily_accepts` 数据库表记录每日接受情况
  - DB 唯一约束防并发重复接受
  - 超过限制时提示「今日已接受过道具，每天限一次」
- 新增 5 个每日接受限制测试

### 测试
- 全量 304 测试通过

## [3.2.0] - 2026-08-24

### 新增
- **祷词触发觐见分变动** — 玩家每天第一次发送命中自己命途祷词的消息时，随机获得 -2 到 +2 的觐见分变化
  - 精确匹配（归一化后 8 个汉字完全相等）
  - 通过 `prayer_trigger_groups` 配置指定生效的群列表（留空不启用）
  - 回复文案可配置（正/负/零三组文案池，随机选取）
  - 回复格式：「神明看到了你的祈祷」+ 命途神名 + 心情描述 + 觐见变化
  - 性能优化：祷词缓存 O(1) 查找、预编译正则、先匹配祷词再解析玩家身份
  - 尊重 `allow_negative_scores` 配置（不允许负分时 clamp 到 0）
  - DB 唯一约束防并发重复触发
- **新增 `prayer_daily_hits` 数据库表** — 记录每日祷词触发，防止重复
- 新增 30 个祷词触发测试

### 测试
- 全量 299 测试通过

## [3.1.0] - 2026-08-23

### 新增
- **QQ 绑定鉴权（防名片冒充）** — 涉及身份的指令（赠送/接受/拒绝道具）改用 `event.get_sender_id()`（协议层注入、不可伪造）作为凭据，替代群名片解析
  - `players` 表新增 `qq_id` 列 + `(group_id, qq_id)` 唯一索引
  - 一个 QQ 在同一群只能绑定一个玩家
- **新增 `绑定QQ` 指令** — 诸神为玩家手动补绑 QQ
  - 支持 `绑定QQ @用户` 或 `绑定QQ <玩家名>` 两种形式
- **新增 `换绑QQ` 指令** — 诸神为玩家替换已绑定的 QQ
  - 支持 `换绑QQ @新QQ用户 <玩家名>` 或 `换绑QQ <玩家名> <新QQ号>` 两种形式
  - 旧 QQ 自动释放；新 QQ 已绑其他玩家时拒绝（需先处理冲突）
- **启动自动迁移** — 首次事件触发后台任务，扫描各群未绑定玩家，名片唯一匹配 → 自动绑定，歧义则跳过
- **赠送超时通知** — 超时退回时向原群发送：`⏰ 赠送超时自动退回：Alice → Bob 的 铁剑[B级] 已退回发送方`

### 改动
- **录入玩家 @用户** — 自动把被 @ 用户的 QQ 绑定给新录入的玩家
- **接受道具 / 拒绝道具** — 权限放开为「所有人」（不再限诸神），默认走 QQ 鉴权；诸神可带参数指定接收玩家（代为操作）
- **查询 / 查询储物空间（非诸神自查）** — 优先 QQ 绑定查找，失败回退名片识别（兼容未绑定的老玩家）
- **只读放行、写入拦截** — 未绑定 QQ 的玩家可用只读指令；写入指令（赠送/接受/拒绝）拒绝并提示「请让诸神使用 绑定QQ」

### 修复
- 接受/拒绝道具命令改用 player_id 作为存储键（与 cmd_gift_item 一致），修复部署后无法找到赠送记录的问题
- `_parse_card_info` 要求名片含职业词才识别玩家名，对无职业词名片失效；改用 `_extract_card_words` + DB 匹配（与自动迁移逻辑统一）

### 测试
- 新增 `tests/test_qq_binding.py`：覆盖 QQ 查找、唯一约束、跨群隔离、register_player qq_id 参数、换绑 QQ（成功/冲突/幂等/首次绑）
- 全量 269 测试通过

## [2.3.0] - 2026-08-21

### 新增
- **群名片自动识别玩家** — 部分指令通过发送者群名片自动识别身份，无需手动输入名字
  - `查询` — 非诸神直接发送即查自己，诸神需指定玩家名
  - `查询储物空间` — 非诸神直接发送查自己，诸神可批量指定
  - `赠送道具` — 发送方自动识别，格式变为 `赠送道具 <接收方名> <道具*数量>`
  - 非诸神用户指定其他玩家时提示"只能查询自己"
- **录入玩家祷词验证** — 录入新玩家时，系统查找群名片对应的群成员，要求本人发送祷词确认，防止冒名注册
  - 60 秒超时机制
  - 祷词匹配忽略所有标点符号和空格
  - 匹配成功后才完成注册
- **设置精华 / 移除精华** — QQ 群管指令，引用消息设置/移除精华
- **新增 `prayer_text_*` 配置项** — 6 个信仰各一个默认 8 字祷词，可在 WebUI 自定义
- **新增 `_resolve_name_from_card()` / `_resolve_player_name()` / `_resolve_target_or_self()` 等核心方法** — 群名片解析与权限感知玩家识别
- **新增 22 个玩家识别测试** — 祷词规范化、群名片词提取

### 改动
- **术语统一** — 帮助文本和文档中"白名单"统一改称"诸神"
- **收回道具失败文案** — 收回失败不再带"已从玩家收回"前缀，成功与失败信息分开显示
- **帮助文本更新** — 反映新指令用法（查询自动识别、赠送道具新格式、录入玩家祷词验证）

### 修复
- **迁移重复道具合并** — 迁移时如遇同名道具（UNIQUE 约束冲突），自动合并数量而非报错
- **迁移扫描所有行** — 迁移逻辑改为扫描所有行，而非仅扫描 `grade IS NULL` 的行
- **增强等级解析** — 支持更多括号格式（如 `（A残级）`、`(B）`）

### 测试
- 总计 **248 个测试全部通过**

## [2.2.0] - 2026-08-10

### 改动
- **储物空间显示格式统一** — 统一为 `道具名 * 数量 |（等级）` 格式
- **D 级道具支持** — D 级道具在迁移时视为无等级处理

## [2.1.0] - 2026-08-09

### 改动
- **道具与等级分离** — 道具名和等级分为两个字段存储，固定等级 SSS/SS/S/A/B/C（大写）
  - 展示格式：`道具名（等级级）`，数量为 1 时不显示 `*1`
  - 收回道具按道具名匹配，不需要指定等级
  - 赠送道具不需要指定等级
  - 批量录入自动解析道具等级
- **赠送道具简化** — 移除确认/取消步骤，直接扣除发送方道具，等待接收方接受/拒绝
  - 移除 `确认赠送` 和 `取消赠送` 命令
  - 移除 `_pending_gifts_send` 状态
- **查询储物空间批量查询** — 支持空格分隔多个玩家名批量查询
- **查询结果排序** — 储物空间按等级从高到低排序（SSS > SS > S > A > B > C > 无等级）
- **新增清除储物空间命令** — 清空全部需加「全部」关键字确认，清除指定道具无需确认
  - `清除储物空间 <玩家名> 全部` — 清空所有道具
  - `清除储物空间 <玩家名> <道具名>` — 清除指定道具（可含等级）

### 数据库
- `player_items` 表新增 `grade TEXT` 列（通过 ALTER TABLE 迁移，幂等可重试）
- 迁移逻辑：旧数据中的等级从 `item_name` 解析并回填到 `grade` 列

### 新增工具函数
- `parse_item_full_name(full_name)` — 从 `'共生噬刃（C级）'` 解析出 `('共生噬刃', 'C')`
- `format_item_display(item_name, grade, quantity)` — 格式化道具展示

### 测试
- 新增 16 个等级相关测试（等级解析、展示格式、清除储物空间、grade 增删改查）
- 总计 **220 个测试全部通过**

## [1.10.0] - 2026-07-19

### 新增
- **玩家状态系统** — 可为玩家添加自定义状态，持续指定天数后自动过期
  - `添加状态 <玩家名> <状态名> <天数>` — 添加状态
  - `移除状态 <玩家名> <状态名>` — 移除指定状态
  - `清除状态 <玩家名>` — 清除所有状态
  - 状态显示在玩家信息卡中（查询时展示）
  - 多状态并存，到期自动隐藏
  - 定时清理过期状态记录
- **QQ 群管命令静默化** — 操作成功不发消息，错误情况保留提示
- **白名单自动同步** — 配置 `auto_whitelist_group` 后，指定群成员进出自动增删白名单
- **同步白名单命令** — 扫描当前群成员批量添加白名单
- 新增 `player_statuses` 数据库表
- 新增 17 个状态系统测试

## [1.9.0] - 2026-07-18

### 新增
- **储物空间系统** — 玩家道具库，支持道具名带品级（如 `共生噬刃（C级）`）
  - 批量录入时自动解析 `【获得道具：名称】` 格式，同步录入道具
  - `查询储物空间 <玩家名>` — 查看玩家道具
  - `赐予道具 <玩家名> <道具*数量> ...` — 赐予多个道具（空格分隔）
  - `收回道具 <玩家名> <道具*数量> ...` — 收回道具（不指定数量则全部收回）
  - 道具数量累加机制，同一道具多次获得自动叠加
- 新增 `player_items` 数据库表
- 新增 20 个道具系统测试

## [1.8.0] - 2026-07-17

### 新增
- **QQ 群管指令** — 白名单权限的群管理命令，使用 `w` 前缀避免与其他群管插件冲突
  - `w禁言` / `w解禁` / `w踢人` / `w改名` / `w撤回` / `w全员禁` / `w全员解`
  - 复用信仰天梯白名单权限系统
  - 需要 aiocqhttp (OneBot11) 协议，机器人需有群管理员权限
- 新增 `qq_admin_handle.py` — QQ 群管命令 Mixin 模块

## [1.7.0] - 2026-07-07

### 新增
- **弃誓指令** (`弃誓 <玩家名> [新信仰]`) — 标记弃誓者，可配置6种信仰的弃誓文本
- **立誓指令** (`立誓 <玩家名> <信仰>`) — 独立设置信仰
- **弃誓者标记** — 排行榜/查询/图片中名字旁显示`(弃誓者)`
- **管理员清除弃誓** — `天梯榜管理 clearoath <玩家名>`
- **录入玩家默认分数** — 可省略分数参数，使用默认值（登神之路1000/觐见100）

### 改动
- 所有显示文本"天梯"→"登神之路"（变量名不变）
- `设置职业` 仅设置职业，不再同时设置信仰
- 信仰管理拆分为独立的`立誓`指令

## [1.6.0] - 2026-07-03

### 修复
- 图片配置 `image_format`/`image_quality` 现在实际生效（之前被忽略，永远输出 PNG）
- 批量录入增加冷却时间检查，防止无限制刷入
- `_render_and_send` 改用 `is_ladder` 参数代替函数指针比较
- `register_player` 不再直接访问 DB 私有属性 `_db`，新增公开 `commit()` 方法
- 帮助文本中"批量录入"改为使用配置变量，与其他命令一致
- 删除 `image_renderer.py` 中未使用的 `template_dir`/`_bg_image_path`/`_bg_image_cache`
- 添加字体缓存，避免每次渲染重复创建字体对象
- 删除 `scheduler_service.py` 中无意义的 `_()` i18n 占位函数
- 删除 `_push_image_mode` 未使用的 `config` 参数

## [1.5.0] - 2026-07-03

### 新增
- **批量录入指令** (`/批量录入`, `/batch`, `/bl`) — 粘贴游戏结算文本自动解析并批量更新积分
  - 支持格式：`【玩家：XXX】【登神之路+X】【觐见之梯+Y】`
  - 兼容 `登神之路` 和 `登神指路` 两种写法
  - 不存在的玩家自动跳过并提示
  - 需要白名单或管理员权限
- **图片输出模式** — 排行榜支持以图片形式展示
  - 新增 `输出模式 text|image` 指令（仅管理员）
  - 支持全局默认模式和群级独立设置
  - 图片渲染失败时自动降级为文本
- **每日推送图片支持** — 定时推送可配置为图片模式

### 改动
- `ladder_service.py` — 新增 `parse_batch_scores()`, `batch_add_scores()`, `get_leaderboard_players()`, `get_pilgrimage_leaderboard_players()`
- `main.py` — 新增 `cmd_batch_add_score()`, `cmd_output_mode()`, `_render_and_send()` 辅助方法
- `scheduler_service.py` — 支持图片模式推送，新增 `_push_image_mode()`
- `db_manager.py` — 新增 `group_settings` 表，`get_group_output_mode()`, `set_group_output_mode()`, `purge_old_score_history()`
- `message_formatter.py` — 帮助信息增加批量录入和输出模式说明
- `_conf_schema.json` — 新增 `output_mode`, `image_quality`, `image_format` 配置项

### 新增文件
- `image_renderer.py` — PIL 本地图片渲染器（渐变背景、金/银/铜排名边框、职业/信仰徽章）
- `fonts/DouyinSansBold.otf` — 内置 CJK 字体
- `templates/leaderboard.html` — 天梯榜 HTML 模板
- `templates/pilgrimage.html` — 觐见榜 HTML 模板
- `tests/test_batch_entry.py` — 批量录入测试（11个）
- `tests/test_image_renderer.py` — 图片渲染测试（8个）
- `tests/test_group_settings.py` — 群设置测试（9个）

### 测试
- 总计 **140 个测试全部通过**
## [1.4.5] -
# 实现计划：信仰天梯插件图片输出功能

## 概述
为信仰天梯排行榜插件添加图片输出能力，支持天梯榜、觐见榜和每日推送以图片形式展示。

## 一、需要修改的文件

### 1. _conf_schema.json - 添加配置项
添加 output_mode、image_quality、image_format 三个配置项

### 2. 新建 templates/ 目录和 HTML 模板
- templates/leaderboard.html - 天梯榜模板
- templates/pilgrimage.html - 觐见榜模板

### 3. 新建 image_renderer.py - 图片渲染服务
封装 AstrBot 的 html_render API，提供图片渲染能力

### 4. ladder_service.py - 添加获取玩家列表方法
- get_leaderboard_players() - 返回 Player 列表用于图片渲染
- get_pilgrimage_leaderboard_players() - 返回 Player 列表

### 5. main.py - 主要改动
- __init__ 中初始化 ImageRenderer
- cmd_ladder() - 支持图片输出模式
- cmd_pilgrimage() - 支持图片输出模式
- 新增 cmd_output_mode() - 切换输出模式命令
- initialize() - 修改 send_to_group 回调支持图片发送

### 6. scheduler_service.py - 支持图片推送
- __init__ 添加 get_leaderboard_players 和 image_renderer 参数
- _do_daily_push() - 根据 output_mode 选择文本或图片推送

### 7. message_formatter.py - 更新帮助信息
format_help() 中添加输出模式说明

## 二、实施步骤

Phase 1: 基础设施
1. 创建 templates/ 目录
2. 创建 leaderboard.html 和 pilgrimage.html 模板
3. 创建 image_renderer.py

Phase 2: 配置扩展
4. 修改 _conf_schema.json 添加新配置项

Phase 3: 服务层修改
5. 修改 ladder_service.py 添加获取玩家列表方法
6. 修改 scheduler_service.py 支持图片推送

Phase 4: 主插件修改
7. 修改 main.py 初始化 ImageRenderer
8. 修改 cmd_ladder() 和 cmd_pilgrimage()
9. 添加 cmd_output_mode() 命令
10. 修改 initialize() 中的 send_to_group 回调

Phase 5: 帮助和测试
11. 更新 format_help() 帮助信息
12. 创建 tests/test_image_renderer.py

## 三、关键设计决策

1. 输出模式：全局配置，默认 text 保持向后兼容
2. 图片格式：默认 jpeg，可配置 png
3. 降级策略：图片渲染失败时自动降级为文本
4. 权限控制：输出模式切换仅限管理员
5. 调度推送：支持图片模式，发送时先发文本标题再发图片
