# 更新日志

## [3.6.6] - 2026-09-13

### 重构
- **指令实现体全部按职责拆分出去**，`main.py` 从 2345 行降到 **866 行**（-63%）。25 个 handler 的实现体移入 `commands/` 下 7 个新 mixin：

  | 模块 | 职责 | 含指令 |
  |---|---|---|
  | `scoreboard.py` | 排行榜展示 | 天梯榜、觐见榜 |
  | `score.py` | 积分录入 | 录入积分、批量录入 |
  | `player.py` | 玩家档案与身份 | 录入玩家、检测玩家、绑定QQ、换绑QQ、设置职业、立誓、弃誓 |
  | `inventory.py` | 储物空间与状态 | 赐予道具、收回道具、清除储物空间、添加/移除/清除状态 |
  | `gift.py` | 道具赠送 | 赠送道具、接受道具、拒绝道具 |
  | `admin.py` | 管理 | 天梯榜管理、白名单、同步白名单、天梯榜帮助、白名单自动同步事件 |
  | `prayer.py` | 祷词触发 | 祷词监听 |

  （`query.py`、`shared.py` 为上一版已拆出的查询与通用发送）

- 约定不变：`@filter.command` / `@filter.regex` 装饰器与指令注册**全部留在 main.py**，mixin 里只有实现体。原因写在 `commands/__init__.py`：AstrBot 只扫描插件类自身的方法注册指令，装饰器放进 mixin 会被沿 MRO 重复扫到，导致重复注册并中断后续指令的注册
- 只被单个模块使用的辅助方法随模块搬走（如 `_get_valid_pending_gift` → gift、`_build_prayer_cache` → prayer）；跨模块共用的基础设施（身份识别、名片解析、配置读取）仍留在 main.py，由实现体通过 `self.` 调用
- 新增 `text_utils.py`：CQ 码 / @ 提及 / 名片标签等正则集中一处（此前散落在 main.py 顶部、多个模块都要引用），并提供共用的 `strip_mentions()`

### 修复
- **`录入玩家` 里一段引用已删变量的死代码** — 计算 `member_id` / `target_card` 的代码块在上次删除「对应群名片」提示时漏下，而它依赖的 `target_member` 已不存在，走到该分支会抛 `NameError`。拆分后由静态扫描发现并删除

### 测试
- 全量 345 测试通过
- 另做三项结构性验证：桩模块导入确认 34 个指令零重复、每个委托都指向真实存在的实现体、未定义名扫描无发现

## [3.6.5] - 2026-09-13 - 2026-09-13

### 优化
- **「录入玩家」回复精简** — 按需求去掉「（对应群名片：X，QQ: Y）」与「已自动绑定你的 QQ，后续可使用需鉴权的指令」两句；顺带省掉一次为拼这行而做的群成员列表查询
- **回复中的「命途」改为「信仰」，并带上具体信仰** — 形如 `职业: 战士 | 信仰：生命 | 繁荣`（只给命途时省略后者）。渲染复用玩家卡片同一函数 `format_faith_line`，避免两处各写一份再漂移

### 修复
- **具体信仰此前被解析出来就丢弃** — 名片解析只保留命途（如"生命"），具体信仰（如"繁荣"）虽然从标签里读到了却没有返回，也没落库，导致查询/文案/主题消息都用不上。现 `_parse_card_info` 一并返回并写入玩家记录（与职业、命途同一事务）
- **按信仰抽取的注册文案从未生效** — `FAITH_MESSAGES` 的键是 16 个具体信仰，而代码用 6 个命途去查，永远查不到、只能退回通用文案（CHANGELOG 3.5.0 的「录入玩家仪式化」实际没兑现）。现按具体信仰取
- **名片解析漏用"单独出现的具体信仰词"** — 这类词原先把从玩家名里剔除后就丢掉，现在会用来补全信仰与命途
- **`set_player_specific_faith` 可能清空命途** — 传入无法识别的信仰时 `FAITH_TO_PATH.get()` 返回 None，会把已有命途写成 NULL；现只在映射存在时才覆盖命途

### 测试
- 新增 `TestRegisterReply` / `TestFaithLineFormatter`：覆盖信仰行渲染、具体信仰落库、按信仰取文案、以及"回复不再承诺取消录入"
- **全量 345 测试通过**

## [3.6.4] - 2026-09-13 - 2026-09-13

### 修复
- **`录入玩家` 使用 @ 时丢失注册结果** — @ 分支把 `register_player` 返回的结果整条丢弃，只回一句提醒，导致用 @ 录入时看不到录入的玩家名、职业、命途、**刚录入的两个分数**以及信仰专属文案（同一句结果在非 @ 路径是正常发出的）。现两条路径回复统一：结果始终给出，@ 仅作为前置提及并说明已自动绑定 QQ
- **去掉一句不存在的后果** — @ 路径的提醒原文为「请及时进行谕行（或说出祷词），否则将取消录入」，但"祷词确认后取消录入"的机制在早前版本已移除（全仓库已无任何实现），该承诺属于历史残留，现改为「已自动绑定你的 QQ，后续可使用需鉴权的指令」

### 测试
- 全量 337 测试通过

## [3.6.3] - 2026-09-13 - 2026-09-13

全量代码审查后的一轮修复。多数问题此前不会立刻显形（需要并发、负数量、跨 8 小时时区边界或特定数据状态才触发），但都属于会静默损坏数据或让功能形同虚设的类型。

### 修复
- **负数量道具可凭空造道具** — `赠送道具 Bob 铁剑 -1` 之前会让发送方库存 +1（SQL 里减法变加法）。现在解析层与服务层都要求数量为正整数
- **批量查询带状态的玩家必定崩溃** — `get_statuses_for_players` 用 naive 时间减 aware 时间，抛 `TypeError`，用户得不到任何回复
- **状态提前 8 小时消失/被清理** — `expire_at` 以 UTC 写入，但查询与清理用 UTC+8 比较。现全链路统一 UTC
- **过期状态清理从未落盘** — `purge_expired_statuses` 没有 commit，调度器直接调用它；连接关闭后删除丢失，还会被任意一次 rollback 连带撤销
- **并发注册/绑定会抛异常** — 同一玩家名并发录入、同一 QQ 并发绑定都会抛 `IntegrityError` 而不是返回失败；QQ 唯一索引此前只在新增列时创建，半应用状态的库会永久失去唯一性约束
- **「注册回滚」是空操作** — 各子操作自带 commit，QQ 绑定失败时玩家/职业/积分历史其实已经落库，提示却声称已回滚。现四步共用一个事务
- **改名并发可产生同名玩家** — `player_name` 无唯一约束，「先查重名再 UPDATE」在并发下会双双重名成功；现把重名判断并入 UPDATE 语句
- **批量录入的事务失败被误报** — 回滚时返回的名单会被展示成「以下玩家不存在，已跳过」，掩盖真实数据库错误
- **赠送道具静默丢弃其余道具** — `赠送道具 Bob 铁剑 2 药水 3` 只处理第一件，玩家以为都送出了
- **数据库错误静默** — `get_message` / `INPUT_ERRORS` 两处未定义名会让查询、绑定、设职业、赐予道具在特定分支抛 `NameError`
- **删除玩家/清空群数据残留附属记录** — `pending_gifts` / `prayer_daily_hits` / `gift_daily_accepts` 未清理，同名玩家重新录入后会继承旧的待领取赠送与当日配额
- **同名不同等级的道具无法共存（数据保真）** — `player_items` 主键不含 `grade`，给已持有「铁剑」的玩家发「铁剑（A级）」会把等级并进无等级那一行后丢弃，之后按等级赠送/扣除都报"没有道具"。现将主键改为 `(group, player, name, grade)` 并自动迁移旧库，等级按行独立保存；`收回道具` 也改为按等级精确匹配（未写等级时优先收回无等级那一行）
- **备份可能是撕裂副本** — 备份从 `shutil.copy2` 直接拷活动中的 `.db` 文件改为 SQLite `VACUUM INTO` 一致性快照（旧方式不拷 `-journal`，可能拿到含未提交数据的副本）
- 其他：`白名单 add` 未校验信仰、`换绑QQ` 接受非数字、`@全体成员` 被当成真实 QQ 写入绑定、储物空间彩蛋配置为空列表时 `IndexError`、`检测玩家` 绑定失败仍报成功

### 优化
- **调度器的「每日」任务实际每小时执行一次** — 等于每天产生 24 份备份并把清理跑 24 遍。现按北京日期判断，启动后执行一次
- **冷却在参数校验前被占用** — 批量录入/立誓/弃誓写错格式会白烧 600 秒冷却，改对了也发不出去
- **重置单个玩家/改名/改职业后排行榜缓存未失效** — 榜单最长 30 秒内仍显示旧数据
- **弃誓文案的配置键错位** — 代码拼的是 `oath_text_<命途>`（6 个），而配置里是 `oath_text_<具体信仰>`（16 个），导致 16 个键全部失效、永远用兜底文案
- **诸神信仰未取配置值** — 只在 WebUI 配置里填了信仰的诸神，执行群管操作时退回通用文案
- **权限与冷却提示统一** — 此前存在「唯执棋者」「区区凡人」等 3 种写法，现全部走 `messages.py`
- **群管批量操作的结果汇报** — 解禁失败此前完全静默（无法区分「已解禁」和「没执行」）；禁言/踢人部分成功时只报错误
- `upsert_player` 的返回值此前因「名字是否变化」而来自不同来源（新建时返回分数为 0 的临时对象，改名时返回数据库真值）
- 自动白名单监听注册失败改为显式告警（此前静默跳过，表现为功能看似开启却从不同步）
- 自动补全信仰后同步内存对象，祷词首次触发不再被丢弃
- 批量录入单个道具数量上限 9999（`*2000000` 展开会占用数十 MB 内存）
- **破坏性操作增加二次确认** — `天梯榜管理 清空` / `全部重置` 需在参数后加「确认」，避免一条误发删光全群数据
- **参数中的 @ 提及文本统一剥离** — aiocqhttp 会把 `"@昵称(QQ)"` 拼进 message_str，此前 `录入积分 @张三 100 50` 这类写法会把目标解析成 `"@张三(12345)"` 而报玩家不存在
- **并列成绩的名次口径统一** — 批量查询此前用序号排名，同一成绩会排出不同名次，与单查的"严格领先计数"不一致
- **旧数据迁移补齐单一 `*N` 后缀** — `糖果*3` 这类名字此前不会被清理（文档却写着会），现与 `护身符（C级）*2`、`礼盒*2*3` 一并处理；两份重复的迁移实现合并为一份

### 重构
- 道具参数解析抽为 `item_utils.parse_item_args`（纯函数，不依赖 AstrBot，测试可直接调用）；`main.py` 仅转发
- 删除无调用者的 `should_trigger_now`、`get_nickname` 与未使用的 `ban_time` 参数
- `active_groups` 表及 `register_active_group` / `get_active_groups`（无任何生产调用）
- `format_whitelist`（仅测试使用，生产走 `format_whitelist_combined`）
- `db_manager.backup_database` / `cleanup_old_backups`（与调度器实现重复）
- 补齐 23 处缺失的函数 docstring

### 文档
- README 修正 12 处与代码不符的描述：天梯榜/觐见榜实际需要诸神权限、`帮助` 别名并不存在、`检测玩家` 不支持 @、赠送道具走 QQ 绑定而非名片识别、`query_cooldown_seconds` 默认 5（原写 600）、`backup_retention_days` 默认 7（原写 30）等
- 配置 schema 说明补充：`cmd_*` 仅影响帮助文案与祷词前缀过滤（不能真正改名）、`oath_text_*` 按具体信仰匹配

### 测试
- 新增 `tests/test_review_fixes.py`：23 个用例覆盖时区、并发、负数量、级联删除、改名冲突、
  按等级独立库存与旧库主键迁移
- **全量 337 测试通过**

## [3.6.2] - 2026-09-13

### 修复
- **道具数量标记后接等级括号时解析失败** — 道具解析此前有三份各自独立的实现，且都要求 `*数量` 必须位于整段末尾：
  - 「批量录入」中 `测试*10（b）` 会被当成名字叫 `测试*10（b）` 的无等级道具（数量 1）
  - 「赐予道具」「赠送道具」中 `道具*数量` 写法整段被当作名字，`测试（b）*10` 同样变成名为 `测试（b）*10` 的道具

  现将数量解析统一为 `item_utils.extract_item_quantity`，三处共用，数量标记位置不再受限：
  - `测试*10（b）` 与 `测试（b）*10` 均解析为 **10 个 B 级「测试」**
  - 同时识别本插件展示格式的乘号，`共生噬刃×3（C级）` 可直接复制粘贴
- `收回道具` 的道具名模式同样改用统一解析（此前 `*数量` 也必须写在末尾，写在等级前会解析失败）

### 优化
- 道具数量解析由三份实现（批量录入 / 赐予道具与赠送道具 / 收回道具）合并为一份，避免继续各自漂移
- 指令用法文案与 README 补充 `道具*数量`、`道具×数量` 写法说明

### 测试
- 新增 `extract_item_quantity` 单元测试，覆盖数量标记在等级前后、`*` 与 `×`、非数字 `*` 等情形
- 新增批量录入两种数量位置的端到端测试，并断言最终落库为 (测试, B, 10)
- 全量 318 测试通过

## [3.6.1] - 2026-09-13

### 修复
- **赠送道具完全不可用** — 此前走的是旧实现，同时存在 `parse_item_full_name` 未导入（NameError）与 `deduct_item` 参数错位两处缺陷，任何赠送都会失败；已改用此前被误粘进 `拒绝道具` 里的新实现，两处缺陷一并消失
- **赠送内存缓存泄漏** — 超时退款由调度器完成时没有清理内存缓存，该接收方此后在整个运行期间都收不到新的赠送；现由调度器退款回调清理，且判定改以数据库为准
- **并发下道具可被复制或丢失** — 道具扣减原本是「读-改-写」，并发赠送同一件道具时库存 1 能被成功送出 2 次；待处理赠送原本用 `INSERT OR REPLACE` 保存，第二笔会覆盖第一笔、而第一笔道具已扣除。现改为单条原子扣减 + 拒绝覆盖写入（写入失败自动退还发送方）
- **接受/拒收赠送可能重复发放** — 改为「先认领（删除记录）再发放」，并发下只有一方能成功
- **每日接受次数上限可被绕过** — `record_gift_accept` 的返回值此前被忽略，写库失败也照常发放
- **群主/管理员可能被误禁言或误踢** — 群成员信息接口失败时角色默认按普通成员处理，导致保护失效；现改为取不到角色就跳过该目标
- **`白名单 list` / `view` 不显示运行时添加的诸神** — 此前只显示 WebUI 配置项，用 `白名单 add` 添加的人一个都不显示（但权限实际已生效），还会提示"列表为空、可通过指令 add 添加"；现分「WebUI 配置」「运行时添加」两段显示并给出各自数量
- **`白名单 setfaith` 拒绝了所有合法信仰** — 校验误用 6 命途（`VALID_PATHS`）而非 16 具体信仰（`VALID_FAITHS`），导致 README 所写的 `白名单 setfaith <QQ> 湮灭` 这类用法全部被判为"无效信仰"；而信仰主题文案（`FAITH_MESSAGES`）恰恰只按 16 具体信仰索引，3.6.0 条目中记录的正是这一项修复，但代码里并未生效
- **数据库迁移中的未保护导入** — `db_manager` 的 `faith` 列迁移分支内重复执行了一次无兜底的 `from astrbot.api import logger`，使无 AstrBot 环境下所有测试在初始化阶段报错。**3.6.0 条目中记为"162 个 Windows 文件锁定错误"的其实是这一项**，与文件锁无关

### 新增
- **祷词真实改分开关** — 新增配置项 `prayer_score_enabled`（默认关闭）。关闭时保持原行为（展示随机结果并提示"本次结果暂时不会影响实际分数"）；开启后按匹配 -2~+2、渎神 -2~0 真实计入觐见分，回复中不再出现该提示

### 重构
- **指令实现体按职责拆分（试点）** — 新建 `commands/` 包，把 `查询` 与 `查询储物空间` 的实现体移入 `commands/query.py` 的 `QueryCommandsMixin`，`main.py` 只保留 `@filter.command` 装饰器与一行委托。装饰器一律不进 mixin：AstrBot 只扫描插件类自身的方法注册指令，把装饰器放进 mixin 会被沿 MRO 重复扫到，导致重复注册并中断后续指令的注册（表现为部分指令回复两次、另一些完全不触发）
- **合并转发逻辑收口** — 天梯榜 / 觐见榜 / 储物空间三处逐字重复的 `send_group_forward_msg` 逻辑合并为 `commands/shared.py` 的 `SharedSendMixin._send_forward_text`

### 优化
- **道具扣减改为单条原子 SQL** — 顺带修正 `收回道具` 超出持有数量时谎报收回数量的问题（此前会报"已收回 N 个"而实际只清空了库存）
- 删除重复定义：`ladder_service.take_items`（两份逐字相同）、`qq_admin_handle._check_permission`（两份逐字相同）、`ladder_service` 的重复 import
- 删除 `main.py` 中重复的 `cmd_accept_gift` / `cmd_reject_gift` 死代码共 225 行（含误粘进 `拒绝道具` 的赠送实现）
- 删除 `main.py` 中 5 个无引用的导入与 1 个无引用正则
- **诸神代收道具不再受每日上限约束** — 此前诸神用「接受道具」代收同样受 `gift_daily_accept_limit` 限制，且会占用接收方的当日配额（导致接收方自己反而无法再接受）；现诸神代收既不受上限约束，也不计入接收方配额
- 版本号与 `metadata.yaml` 对齐（此前 `@register` 写 2.1.0、metadata 写 v3.6.0），插件描述同步移除已删除的功能

### 移除
- **图片输出子系统**（早已无任何调用）：`image_renderer.py`、`templates/*.html`、`fonts/`、`Pillow` 依赖、`output_mode` / `image_quality` / `image_format` 配置项、`group_settings` 表及其访问函数
- `ladder_service` 中无调用者的 `get_leaderboard_players` / `get_pilgrimage_leaderboard_players` / `get_effective_output_mode` / `get_player_card_text`

### 测试
- 新增 `commands/` 包约定守卫测试：禁止在 mixin 中出现 `filter.` 装饰器、mixin 方法必须以 `_` 开头，防止重新引入指令重复注册
- 新增赠送并发语义测试：待处理赠送拒绝覆盖写入、删除的认领语义、超时退款幂等且回调清理缓存、并发扣减只有一个成功
- 新增祷词开关文案测试
- **全量 306 测试通过**（此前因上述未保护导入，实际为 151 通过 / 162 报错）

## [3.6.0] - 2026-08-29

### 新增
- **统一文案库** — 新建 `messages.py` 和 `faith_messages.py`，所有消息集中管理，支持变量替换和随机选取
- **信仰专属随机文案** — 16 信仰 × 6 种操作（禁言/踢人/赐予/收回/录入/清除弃誓），每种操作多条随机文案
- **诸神操作显示信仰身份** — 白名单支持 `faith` 字段，有信仰的诸神执行操作时显示对应神明文案（如"【沉默】降下神赐"）
- **白名单神明系统** — `白名单 add <QQ> [信仰]` / `setfaith` / `removefaith` / `view`，白名单绑定具体信仰
- **群管操作信仰主题化** — 禁言/踢人成功显示信仰专属文案，非静默
- **储物空间查询彩蛋** — 可配置概率触发趣味消息（如"【湮灭】令使路过你的储物空间..."），不影响实际道具
- **检测玩家支持 @** — 诸神可通过 `检测玩家 @用户` 查看他人绑定状态

### 优化
- **权限消息统一** — 所有权限拒绝统一为"此等权柄，唯诸神方可执掌。"
- **玩家不存在消息统一** — 统一为"{name}未引起寰宇诸神的注意。"
- **道具显示格式** — `*` 改为 `×`，去掉 `|`，如"共生噬刃×3（C级）"
- **赠送道具格式** — `道具 数量` 分隔（不再是 `道具*数量`），如"赐予道具 张三 铁剑 2 生命药水 3"
- **玩家档案格式** — `信仰：命途 | 具体信仰`，排名改为括号显示
- **排行榜简化** — 只显示命途，不显示具体信仰
- **批量录入文案** — "结算完成，X 人积分已变更"
- **帮助文案去"棋子/棋局"** — 改为"录入新信徒"等信仰主题表述
- **名片解析优化** — 过滤职业词，只保留玩家名用于匹配

### 修复
- **封神之路解析** — 兼容"封神之路"/"登神指路"等变体名称
- **配置 schema** — `inventory_easter_egg` 改为平铺配置项，避免 object.items 导致安装失败
- **白名单信仰字段** — `setfaith` 验证使用 `VALID_FAITHS`（16具体信仰）而非 `VALID_PATHS`（6命途）

### 测试
- 全量 313 测试通过（162 个 Windows 文件锁定错误不影响逻辑）

## [3.5.0] - 2026-08-28

### 新增
- **检测玩家指令** — `检测玩家`（别名：`检测`、`check`），检查当前玩家的绑定状态，未绑定时自动绑定 QQ
- **名片识别自动绑定 QQ** — 玩家首次使用查询/储物空间等指令时，通过名片识别到身份后自动绑定 QQ，后续可直接使用鉴权指令
- **祷词按 16 信仰配置** — 祷词和触发文案从命途级细化到信仰级，每个具体信仰有独立的祷词列表和回复文案
- **玩家具体信仰存储** — DB 新增 `specific_faith` 字段，解析名片 `【】` 时自动保存
- **录入玩家仪式化** — 新玩家录入显示「踏入棋局」仪式化回复
- **沉浸感优化** — 排行榜标题（凡人的阶梯/神明的凝视）、玩家卡片/储物空间/UI 分隔符沉浸化
- **权限/排行榜缓存** — 权限检查缓存 5 分钟、排行榜缓存 30 秒
- **祷词随机打分** — 匹配随机 -2~+2，渎神随机 -2~0，随机到 0 显示宽宏大量

### 优化
- **正则预编译** — CQ 码清理、名片解析等高频正则预编译
- **批量查询支持** — `查询` 指令支持多个玩家名批量查询
- **信仰/命途区分** — 代码层明确区分 6 命途与 16 信仰，配置、缓存、匹配全部按具体信仰

### 移除
- **移除 QQ 迁移代码** — `_maybe_trigger_qq_migration` / `_do_auto_migrate_qq` / `list_unbound_players`，功能已由自动绑定替代
- **移除组白名单支持** — 白名单仅保留 user 类型，简化指令语法

### 修复
- **缓存失效不完整** — 白名单变更/积分重置/删除玩家后立即失效对应缓存
- **时区问题** — 每日重置使用北京时间（UTC+8）而非 UTC

### 测试
- 全量 313 测试通过

## [3.4.0] - 2026-08-26

### 变更
- **每日接受道具上限可配置** — 新增 `gift_daily_accept_limit` 配置项（默认 1，0 为不限制）
  - `gift_daily_accepts` 表改为自增主键（支持多条记录/天）
  - `has_gift_accept_today` / `count_gift_accepts_today` 方法配合可配置限制

### 移除
- **移除每日推送功能** — 该功能因 AstrBot `send_to_group` API 格式问题从未正常工作
  - 移除 `scheduler_service.py` 中 `_daily_push_loop` / `_do_daily_push` / `_push_text_mode` / `_push_image_mode` / `_get_effective_output_mode`
  - 移除 `_conf_schema.json` 中 `daily_push_time` / `daily_push_enabled` / `daily_push_groups`
  - 移除 `message_formatter.py` 中 `format_daily_push_*` 函数和 `get_output_mode` 函数
  - 移除 `main.py` 中 `SchedulerService` 构造函数的每日推送参数
  - 移除 README 中每日推送相关文档和配置表条目

### 测试
- 全量 304 测试通过

## [3.3.0] - 2026-08-24

### 新增
- **每日接受道具限制** — 每位玩家每天只能接受一次道具（防止刷道具）
  - 新增 `gift_daily_accepts` 数据库表记录每日接受情况
  - DB 唯一约束防并发重复接受
  - 超过限制时提示「今日已接受过道具，每天限一次」
- 新增 5 个每日接受限制测试

### 测试
- 全量 304 测试通过

## [3.2.0] - 2026-08-24

### 新增
- **祷词触发觐见分变动** — 玩家每天第一次发送命中自己命途祷词的消息时，随机获得 -2 到 +2 的觐见分变化
  - 精确匹配（归一化后 8 个汉字完全相等）
  - 通过 `prayer_trigger_groups` 配置指定生效的群列表（留空不启用）
  - 回复文案可配置（正/负/零三组文案池，随机选取）
  - 回复格式：「神明看到了你的祈祷」+ 命途神名 + 心情描述 + 觐见变化
  - 性能优化：祷词缓存 O(1) 查找、预编译正则、先匹配祷词再解析玩家身份
  - 尊重 `allow_negative_scores` 配置（不允许负分时 clamp 到 0）
  - DB 唯一约束防并发重复触发
- **新增 `prayer_daily_hits` 数据库表** — 记录每日祷词触发，防止重复
- 新增 30 个祷词触发测试

### 测试
- 全量 299 测试通过

## [3.1.0] - 2026-08-23

### 新增
- **QQ 绑定鉴权（防名片冒充）** — 涉及身份的指令（赠送/接受/拒绝道具）改用 `event.get_sender_id()`（协议层注入、不可伪造）作为凭据，替代群名片解析
  - `players` 表新增 `qq_id` 列 + `(group_id, qq_id)` 唯一索引
  - 一个 QQ 在同一群只能绑定一个玩家
- **新增 `绑定QQ` 指令** — 诸神为玩家手动补绑 QQ
  - 支持 `绑定QQ @用户` 或 `绑定QQ <玩家名>` 两种形式
- **新增 `换绑QQ` 指令** — 诸神为玩家替换已绑定的 QQ
  - 支持 `换绑QQ @新QQ用户 <玩家名>` 或 `换绑QQ <玩家名> <新QQ号>` 两种形式
  - 旧 QQ 自动释放；新 QQ 已绑其他玩家时拒绝（需先处理冲突）
- **启动自动迁移** — 首次事件触发后台任务，扫描各群未绑定玩家，名片唯一匹配 → 自动绑定，歧义则跳过
- **赠送超时通知** — 超时退回时向原群发送：`⏰ 赠送超时自动退回：Alice → Bob 的 铁剑[B级] 已退回发送方`

### 改动
- **录入玩家 @用户** — 自动把被 @ 用户的 QQ 绑定给新录入的玩家
- **接受道具 / 拒绝道具** — 权限放开为「所有人」（不再限诸神），默认走 QQ 鉴权；诸神可带参数指定接收玩家（代为操作）
- **查询 / 查询储物空间（非诸神自查）** — 优先 QQ 绑定查找，失败回退名片识别（兼容未绑定的老玩家）
- **只读放行、写入拦截** — 未绑定 QQ 的玩家可用只读指令；写入指令（赠送/接受/拒绝）拒绝并提示「请让诸神使用 绑定QQ」

### 修复
- 接受/拒绝道具命令改用 player_id 作为存储键（与 cmd_gift_item 一致），修复部署后无法找到赠送记录的问题
- `_parse_card_info` 要求名片含职业词才识别玩家名，对无职业词名片失效；改用 `_extract_card_words` + DB 匹配（与自动迁移逻辑统一）

### 测试
- 新增 `tests/test_qq_binding.py`：覆盖 QQ 查找、唯一约束、跨群隔离、register_player qq_id 参数、换绑 QQ（成功/冲突/幂等/首次绑）
- 全量 269 测试通过

## [2.3.0] - 2026-08-21

### 新增
- **群名片自动识别玩家** — 部分指令通过发送者群名片自动识别身份，无需手动输入名字
  - `查询` — 非诸神直接发送即查自己，诸神需指定玩家名
  - `查询储物空间` — 非诸神直接发送查自己，诸神可批量指定
  - `赠送道具` — 发送方自动识别，格式变为 `赠送道具 <接收方名> <道具*数量>`
  - 非诸神用户指定其他玩家时提示"只能查询自己"
- **录入玩家祷词验证** — 录入新玩家时，系统查找群名片对应的群成员，要求本人发送祷词确认，防止冒名注册
  - 60 秒超时机制
  - 祷词匹配忽略所有标点符号和空格
  - 匹配成功后才完成注册
- **设置精华 / 移除精华** — QQ 群管指令，引用消息设置/移除精华
- **新增 `prayer_text_*` 配置项** — 6 个信仰各一个默认 8 字祷词，可在 WebUI 自定义
- **新增 `_resolve_name_from_card()` / `_resolve_player_name()` / `_resolve_target_or_self()` 等核心方法** — 群名片解析与权限感知玩家识别
- **新增 22 个玩家识别测试** — 祷词规范化、群名片词提取

### 改动
- **术语统一** — 帮助文本和文档中"白名单"统一改称"诸神"
- **收回道具失败文案** — 收回失败不再带"已从玩家收回"前缀，成功与失败信息分开显示
- **帮助文本更新** — 反映新指令用法（查询自动识别、赠送道具新格式、录入玩家祷词验证）

### 修复
- **迁移重复道具合并** — 迁移时如遇同名道具（UNIQUE 约束冲突），自动合并数量而非报错
- **迁移扫描所有行** — 迁移逻辑改为扫描所有行，而非仅扫描 `grade IS NULL` 的行
- **增强等级解析** — 支持更多括号格式（如 `（A残级）`、`(B）`）

### 测试
- 总计 **248 个测试全部通过**

## [2.2.0] - 2026-08-10

### 改动
- **储物空间显示格式统一** — 统一为 `道具名 * 数量 |（等级）` 格式
- **D 级道具支持** — D 级道具在迁移时视为无等级处理

## [2.1.0] - 2026-08-09

### 改动
- **道具与等级分离** — 道具名和等级分为两个字段存储，固定等级 SSS/SS/S/A/B/C（大写）
  - 展示格式：`道具名（等级级）`，数量为 1 时不显示 `*1`
  - 收回道具按道具名匹配，不需要指定等级
  - 赠送道具不需要指定等级
  - 批量录入自动解析道具等级
- **赠送道具简化** — 移除确认/取消步骤，直接扣除发送方道具，等待接收方接受/拒绝
  - 移除 `确认赠送` 和 `取消赠送` 命令
  - 移除 `_pending_gifts_send` 状态
- **查询储物空间批量查询** — 支持空格分隔多个玩家名批量查询
- **查询结果排序** — 储物空间按等级从高到低排序（SSS > SS > S > A > B > C > 无等级）
- **新增清除储物空间命令** — 清空全部需加「全部」关键字确认，清除指定道具无需确认
  - `清除储物空间 <玩家名> 全部` — 清空所有道具
  - `清除储物空间 <玩家名> <道具名>` — 清除指定道具（可含等级）

### 数据库
- `player_items` 表新增 `grade TEXT` 列（通过 ALTER TABLE 迁移，幂等可重试）
- 迁移逻辑：旧数据中的等级从 `item_name` 解析并回填到 `grade` 列

### 新增工具函数
- `parse_item_full_name(full_name)` — 从 `'共生噬刃（C级）'` 解析出 `('共生噬刃', 'C')`
- `format_item_display(item_name, grade, quantity)` — 格式化道具展示

### 测试
- 新增 16 个等级相关测试（等级解析、展示格式、清除储物空间、grade 增删改查）
- 总计 **220 个测试全部通过**

## [1.10.0] - 2026-07-19

### 新增
- **玩家状态系统** — 可为玩家添加自定义状态，持续指定天数后自动过期
  - `添加状态 <玩家名> <状态名> <天数>` — 添加状态
  - `移除状态 <玩家名> <状态名>` — 移除指定状态
  - `清除状态 <玩家名>` — 清除所有状态
  - 状态显示在玩家信息卡中（查询时展示）
  - 多状态并存，到期自动隐藏
  - 定时清理过期状态记录
- **QQ 群管命令静默化** — 操作成功不发消息，错误情况保留提示
- **白名单自动同步** — 配置 `auto_whitelist_group` 后，指定群成员进出自动增删白名单
- **同步白名单命令** — 扫描当前群成员批量添加白名单
- 新增 `player_statuses` 数据库表
- 新增 17 个状态系统测试

## [1.9.0] - 2026-07-18

### 新增
- **储物空间系统** — 玩家道具库，支持道具名带品级（如 `共生噬刃（C级）`）
  - 批量录入时自动解析 `【获得道具：名称】` 格式，同步录入道具
  - `查询储物空间 <玩家名>` — 查看玩家道具
  - `赐予道具 <玩家名> <道具*数量> ...` — 赐予多个道具（空格分隔）
  - `收回道具 <玩家名> <道具*数量> ...` — 收回道具（不指定数量则全部收回）
  - 道具数量累加机制，同一道具多次获得自动叠加
- 新增 `player_items` 数据库表
- 新增 20 个道具系统测试

## [1.8.0] - 2026-07-17

### 新增
- **QQ 群管指令** — 白名单权限的群管理命令，使用 `w` 前缀避免与其他群管插件冲突
  - `w禁言` / `w解禁` / `w踢人` / `w改名` / `w撤回` / `w全员禁` / `w全员解`
  - 复用信仰天梯白名单权限系统
  - 需要 aiocqhttp (OneBot11) 协议，机器人需有群管理员权限
- 新增 `qq_admin_handle.py` — QQ 群管命令 Mixin 模块

## [1.7.0] - 2026-07-07

### 新增
- **弃誓指令** (`弃誓 <玩家名> [新信仰]`) — 标记弃誓者，可配置6种信仰的弃誓文本
- **立誓指令** (`立誓 <玩家名> <信仰>`) — 独立设置信仰
- **弃誓者标记** — 排行榜/查询/图片中名字旁显示`(弃誓者)`
- **管理员清除弃誓** — `天梯榜管理 clearoath <玩家名>`
- **录入玩家默认分数** — 可省略分数参数，使用默认值（登神之路1000/觐见100）

### 改动
- 所有显示文本"天梯"→"登神之路"（变量名不变）
- `设置职业` 仅设置职业，不再同时设置信仰
- 信仰管理拆分为独立的`立誓`指令

## [1.6.0] - 2026-07-03

### 修复
- 图片配置 `image_format`/`image_quality` 现在实际生效（之前被忽略，永远输出 PNG）
- 批量录入增加冷却时间检查，防止无限制刷入
- `_render_and_send` 改用 `is_ladder` 参数代替函数指针比较
- `register_player` 不再直接访问 DB 私有属性 `_db`，新增公开 `commit()` 方法
- 帮助文本中"批量录入"改为使用配置变量，与其他命令一致
- 删除 `image_renderer.py` 中未使用的 `template_dir`/`_bg_image_path`/`_bg_image_cache`
- 添加字体缓存，避免每次渲染重复创建字体对象
- 删除 `scheduler_service.py` 中无意义的 `_()` i18n 占位函数
- 删除 `_push_image_mode` 未使用的 `config` 参数

## [1.5.0] - 2026-07-03

### 新增
- **批量录入指令** (`/批量录入`, `/batch`, `/bl`) — 粘贴游戏结算文本自动解析并批量更新积分
  - 支持格式：`【玩家：XXX】【登神之路+X】【觐见之梯+Y】`
  - 兼容 `登神之路` 和 `登神指路` 两种写法
  - 不存在的玩家自动跳过并提示
  - 需要白名单或管理员权限
- **图片输出模式** — 排行榜支持以图片形式展示
  - 新增 `输出模式 text|image` 指令（仅管理员）
  - 支持全局默认模式和群级独立设置
  - 图片渲染失败时自动降级为文本
- **每日推送图片支持** — 定时推送可配置为图片模式

### 改动
- `ladder_service.py` — 新增 `parse_batch_scores()`, `batch_add_scores()`, `get_leaderboard_players()`, `get_pilgrimage_leaderboard_players()`
- `main.py` — 新增 `cmd_batch_add_score()`, `cmd_output_mode()`, `_render_and_send()` 辅助方法
- `scheduler_service.py` — 支持图片模式推送，新增 `_push_image_mode()`
- `db_manager.py` — 新增 `group_settings` 表，`get_group_output_mode()`, `set_group_output_mode()`, `purge_old_score_history()`
- `message_formatter.py` — 帮助信息增加批量录入和输出模式说明
- `_conf_schema.json` — 新增 `output_mode`, `image_quality`, `image_format` 配置项

### 新增文件
- `image_renderer.py` — PIL 本地图片渲染器（渐变背景、金/银/铜排名边框、职业/信仰徽章）
- `fonts/DouyinSansBold.otf` — 内置 CJK 字体
- `templates/leaderboard.html` — 天梯榜 HTML 模板
- `templates/pilgrimage.html` — 觐见榜 HTML 模板
- `tests/test_batch_entry.py` — 批量录入测试（11个）
- `tests/test_image_renderer.py` — 图片渲染测试（8个）
- `tests/test_group_settings.py` — 群设置测试（9个）

### 测试
- 总计 **140 个测试全部通过**
## [1.4.5] -
# 实现计划：信仰天梯插件图片输出功能

## 概述
为信仰天梯排行榜插件添加图片输出能力，支持天梯榜、觐见榜和每日推送以图片形式展示。

## 一、需要修改的文件

### 1. _conf_schema.json - 添加配置项
添加 output_mode、image_quality、image_format 三个配置项

### 2. 新建 templates/ 目录和 HTML 模板
- templates/leaderboard.html - 天梯榜模板
- templates/pilgrimage.html - 觐见榜模板

### 3. 新建 image_renderer.py - 图片渲染服务
封装 AstrBot 的 html_render API，提供图片渲染能力

### 4. ladder_service.py - 添加获取玩家列表方法
- get_leaderboard_players() - 返回 Player 列表用于图片渲染
- get_pilgrimage_leaderboard_players() - 返回 Player 列表

### 5. main.py - 主要改动
- __init__ 中初始化 ImageRenderer
- cmd_ladder() - 支持图片输出模式
- cmd_pilgrimage() - 支持图片输出模式
- 新增 cmd_output_mode() - 切换输出模式命令
- initialize() - 修改 send_to_group 回调支持图片发送

### 6. scheduler_service.py - 支持图片推送
- __init__ 添加 get_leaderboard_players 和 image_renderer 参数
- _do_daily_push() - 根据 output_mode 选择文本或图片推送

### 7. message_formatter.py - 更新帮助信息
format_help() 中添加输出模式说明

## 二、实施步骤

Phase 1: 基础设施
1. 创建 templates/ 目录
2. 创建 leaderboard.html 和 pilgrimage.html 模板
3. 创建 image_renderer.py

Phase 2: 配置扩展
4. 修改 _conf_schema.json 添加新配置项

Phase 3: 服务层修改
5. 修改 ladder_service.py 添加获取玩家列表方法
6. 修改 scheduler_service.py 支持图片推送

Phase 4: 主插件修改
7. 修改 main.py 初始化 ImageRenderer
8. 修改 cmd_ladder() 和 cmd_pilgrimage()
9. 添加 cmd_output_mode() 命令
10. 修改 initialize() 中的 send_to_group 回调

Phase 5: 帮助和测试
11. 更新 format_help() 帮助信息
12. 创建 tests/test_image_renderer.py

## 三、关键设计决策

1. 输出模式：全局配置，默认 text 保持向后兼容
2. 图片格式：默认 jpeg，可配置 png
3. 降级策略：图片渲染失败时自动降级为文本
4. 权限控制：输出模式切换仅限管理员
5. 调度推送：支持图片模式，发送时先发文本标题再发图片
